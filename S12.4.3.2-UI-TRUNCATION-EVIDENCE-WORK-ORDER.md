# AutoShutdown 底部状态区域截断取证工作单（识图版 · 修订一）

> 状态：待执行
> 日期：2026-08-13
> 项目根目录：`D:\电脑定时关机重建完整版`
> 执行者：**具备图像识别能力的 AI 模型**
> 前置说明：本工作单是 `S12.4.3.1-UI-TRUNCATION-DIAGNOSIS.md` 的修订版。上一版收到的修改建议存在两处不可接受的问题（详见 §5），本次重写为**取证导向**：先精确定位截断的几何事实，再给最小修改建议。
> 目的：确定主界面底部"任务状态 / 调度服务 / 配置状态 / 最近活动"四个区域被截断的**确切几何原因**，并给出**只改 XAML 布局、经得起代码审查**的修复建议。

---

## 0. 必读：上次建议为何被否决

执行者若拿到上一版的建议，必须理解以下两条**不可用**的原因，避免再次提出：

1. **不得移除 Viewbox 子元素的固定尺寸。** 原布局为：
   ```xml
   <Viewbox Stretch="Uniform" StretchDirection="Both" Margin="12">
       <Grid Width="900" Height="560" Margin="4">
   ```
   `Viewbox` 依赖子元素固定尺寸计算缩放比例。移除 `Width=900 Height=560` 会让 Viewbox 失效、内容可能撑出窗口。**禁止**建议"去掉固定尺寸 + 加 Border + ContentPresenter"这类方案，尤其禁止引入不存在的 `Content="{Binding MainContent}"` 绑定。

2. **不得把问题定位到左侧导航。** 用户报告的区域是**底部状态卡片行 + 最近活动**，不是左侧导航栏的图标对齐。执行者必须先在截图中确认问题区域，再对症下药。

---

## 1. 本次任务目标

回答下面三个问题，每个都必须基于截图给出**可验证的证据**，禁止含糊表述：

1. **截断的精确位置与形态**：底部四个区域是整行被裁，还是个别卡片被裁？被"窗口边缘"裁掉，还是被"上方内容挤压出可视区"？
2. **挤压来源**：如果卡片被上方内容挤出，是哪个上方元素（创建任务卡片 / 当前任务卡片的倒计时数字 / 中间 16px 空隙）占高了？
3. **窗口尺寸相关性**：默认 1080×640 与最小 900×580 下，截断表现是否一致？在 100%/125%/150%/200% 缩放下是否变化？

---

## 2. 截图要求

请向用户索取以下截图（每个都需要完整主窗口，不要裁切）：

1. 默认尺寸 1080×640 的主窗口全景；
2. 最小尺寸 900×580 的主窗口全景；
3. 若条件允许：同一窗口在 125% 与 200% 系统缩放下的对照。

如果已有截图不足，请在汇报中明确列出"还缺哪一张，需要用户补充"。

---

## 3. 取证输出模板（必须逐项填写）

### 3.1 截断形态判定（必答，二选一并给证据）

- [ ] **窗口边缘裁切型**：底部被窗口客户区下边缘硬切。证据：截图中能看到卡片与窗口边框之间无空隙，或卡片内容被窗口下沿切断。
- [ ] **内容挤压溢出型**：底部卡片被上方内容挤出 Viewbox 可视范围。证据：卡片被"削掉"上半部分、能看到卡片内部元素被截断，且上方某元素显得异常高。

**请用截图坐标描述**：例如"最近活动卡片左侧边缘距窗口左边约 X px，顶部被切断处距窗口底部约 Y px"。

### 3.2 挤压来源定位（若为挤压型）

请逐项对照截图与下方代码，指出最可能占高导致挤压的元素：

- [ ] 当前任务卡片的倒计时数字（`FontSize=68`，第 148 行）
- [ ] 创建任务卡片内的下拉框/按钮列高度
- [ ] 状态卡片 `MinHeight=108`（三张卡实际渲染高度）
- [ ] 其他：请具体说明

### 3.3 窗口尺寸与缩放相关性（必答）

| 窗口尺寸 | 缩放 | 是否截断 | 截断区域 | 备注 |
|---|---|---|---|---|
| 1080×640 | 100% | | | |
| 900×580 | 100% | | | |
| 1080×640 | 125% | | | |
| 1080×640 | 200% | | | |

（表格可增删行，但至少包含默认与最小两种尺寸）

---

## 4. 修复建议要求（在取证结论之后给出）

1. **只允许修改** `src\AutoShutdown.App\MainWindow.xaml` 或 `src\AutoShutdown.App\Themes\Controls.xaml` 的布局与样式。
2. 每个建议必须：
   - 给出**具体目标文件与行号**（基于下方代码上下文）；
   - 给出**可直接替换的 XAML 片段**；
   - 说明该改动**如何消除截断**（对应哪个取证结论）；
   - 标注风险（是否引入滚动条、是否改变整体缩放比例、是否影响其他页面）。
3. 若你判断根因是 `Row0` 内容超高导致 `Row2` 被挤出，可考虑但不限于：
   - 给"当前任务"卡片或倒计时数字设置更合理的 `MaxHeight` / 缩小字号；
   - 调整 `Row2` 的行高分配，例如给 Row2 固定高度或 `MinHeight`；
   - 增大 Viewbox 内 Grid 高度（如 560 → 580~600）并相应评估窗口最小高度；
   - 若在最小窗口下才截断，可评估 `MinHeight` 是否需要上调。
4. **禁止**：移除 Viewbox 固定尺寸；引入不存在的数据绑定；大改视觉风格；修改 C#/ViewModel/Core/状态机/电源。

---

## 5. 代码上下文（与上一版一致，供对照）

### 5.1 主窗口与 Viewbox

`src\AutoShutdown.App\MainWindow.xaml`：

```xml
<Window ... Title="电脑自动关机助手" Icon="/assets/icon.ico"
        Width="1080" Height="640" MinWidth="900" MinHeight="580" ...>
```

```xml
<Viewbox Visibility="{Binding IsHomeVisible, Converter={StaticResource BoolToVisibility}}"
         Stretch="Uniform" StretchDirection="Both" Margin="12">
    <Grid Width="900" Height="560" Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Row0 -->
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>  <!-- Row2: 底部状态区 -->
        </Grid.RowDefinitions>

        <!-- Row0 关键元素：当前任务倒计时 -->
        <StackPanel Grid.Row="1" Margin="0,15,0,0">
            <TextBlock Text="{Binding CurrentStateText}" FontSize="15" .../>
            <TextBlock Text="{Binding CountdownText}" FontSize="68" FontWeight="Bold"
                       Foreground="White" Margin="0,4,0,8"> ... </TextBlock>
        </StackPanel>

        <!-- Row2: 状态卡片 + 最近活动 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.18*"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="0.82*"/>
            </Grid.ColumnDefinitions>
            <UniformGrid Grid.Column="0" Columns="3">
                <!-- 任务状态 / 调度服务 / 配置状态 三张卡片 -->
                <Border Style="{StaticResource StatusCardCyanStyle}" Margin="0,0,7,0">...</Border>
                <Border Style="{StaticResource StatusCardBlueStyle}" Margin="7,0">...</Border>
                <Border Style="{StaticResource StatusCardGreenStyle}" Margin="7,0,0,0">
                    <StackPanel HorizontalAlignment="Center">
                        <Border Style="{StaticResource GreenIconCircleStyle}"><TextBlock Text="▣" .../></Border>
                        <TextBlock Text="配置状态" .../>
                        <TextBlock Text="{Binding ConfigStatusText}" TextWrapping="Wrap"
                                   TextAlignment="Center" MaxWidth="120" .../>
                    </StackPanel>
                </Border>
            </UniformGrid>
            <!-- 最近活动 -->
            <Border Grid.Column="2" Style="{StaticResource GlassCardStyle}" Padding="20,16">
                <StackPanel>
                    <TextBlock Text="最近活动" .../>
                    <ListBox ItemsSource="{Binding RecentActivities}"
                             Style="{StaticResource ActivityListStyle}" MaxHeight="112"> ... </ListBox>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Viewbox>
```

### 5.2 关键样式

`src\AutoShutdown.App\Themes\Controls.xaml`：

```xml
<Style x:Key="StatusCardBaseStyle" TargetType="Border" BasedOn="{StaticResource GlassCardStyle}">
    <Setter Property="Padding" Value="9"/>
    <Setter Property="MinHeight" Value="108"/>
</Style>

<Style x:Key="IconCircleBaseStyle" TargetType="Border">
    <Setter Property="Width" Value="40"/>
    <Setter Property="Height" Value="40"/>
    <Setter Property="CornerRadius" Value="20"/>
    <Setter Property="Margin" Value="0,0,0,5"/>
</Style>

<Style x:Key="ActivityListStyle" TargetType="ListBox">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
</Style>
```

---

## 6. 最终汇报格式

汇报必须包含：

1. 三张截图的核对结果（缺哪张明确说明）；
2. §3 取证模板的完整填写（截断形态判定 + 挤压来源 + 尺寸/缩放表格）；
3. 根因结论（一句话）；
4. 修复建议列表（含文件、行号、XAML 片段、对应取证依据、风险）；
5. 明确声明：未建议移除 Viewbox 固定尺寸 / 未引入不存在绑定 / 未修改 C# 或 Core。

完成后停止，等待人工审查。
