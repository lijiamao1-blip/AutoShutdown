# AutoShutdown S12.1 机械执行工作单（归档版）

> 状态：**已完成**  
> 完成日期：2026-08-11  
> 用途：记录 S1—S11 基线构建与测试异常诊断过程，供后续复核。**除非 GPT 明确重新开放，本工作单不得再次执行。**

---

## 0. 阶段目标与硬边界

### 0.1 阶段目标

确认 Release 构建通过后出现的“测试进程异常退出、无正常 TRX 结果”究竟属于项目缺陷、单项测试缺陷，还是 WorkBuddy 命令通道/沙箱环境限制。

### 0.2 硬边界

1. 暂停 S12 后续开发，不进入日志、真实电源、自启动或发布打包。
2. 诊断期间不得修改生产代码、测试断言、状态机、调度器、工作流或电源安全逻辑。
3. 不得通过删除测试、跳过测试、扩大超时或捕获全部异常制造“通过”结果。
4. 不得删除或回滚源码、配置及工作日志。
5. 找到首个稳定复现点后立即停止，只报告证据与最小修复建议。

---

## 1. 原始诊断步骤

1. 终止遗留的 `dotnet`、`testhost`、`vstest` 测试进程。
2. 检查磁盘空间、内存和测试输出目录是否可写。
3. 仅清理项目构建输出 `bin`、`obj`、`TestResults` 后重新 Release 构建；不得触碰源码与配置。
4. 先只列出测试名称，确认测试程序集能够正常发现和加载。
5. 按测试类逐个运行，优先检查涉及 WPF、Windows Forms、Dispatcher、托盘及 App 引用的 S10、S11 测试。
6. 每次记录测试类、启动时间、退出码、通过/失败数量、最后输出及 TRX 是否生成。
7. 如某测试类导致异常，再缩小到单个测试方法。
8. 开启详细诊断日志与 crash dump，保留退出前最后 100 行输出。
9. 单项运行超过 10 分钟且无新输出时终止并标记“疑似卡死”。
10. 最终只汇报首个复现项、退出码、日志、dump、证据、建议修改文件及生产代码是否改动。

---

## 2. 实际诊断结果

### 2.1 WorkBuddy 环境现象

- `FileStorageTests` 在 WorkBuddy 命令通道中出现 testhost 失联或异常退出。
- 涉及真实临时文件写入的方法容易触发现象；纯内存或只读方法可正常运行。
- vstest 诊断日志表现为持续 TCP 轮询失联状态，没有正常测试执行输出。
- 该现象不足以证明 `FileStorage` 或项目生产代码存在缺陷。

### 2.2 用户独立环境验收

用户在普通 Windows PowerShell 中使用项目随附 .NET SDK 独立执行：

```powershell
& "C:\Users\李佳茂\Documents\Codex\2026-08-10\new-chat-5\work\.dotnet-sdk\dotnet.exe" test "D:\电脑定时关机重建完整版\tests\AutoShutdown.Tests\AutoShutdown.Tests.csproj" -c Release --no-build --no-restore --logger "console;verbosity=normal"
```

验收结果：

- Release 全量测试：**278/278 通过**
- 失败：**0**
- 跳过：**0**
- 总耗时：约 **1.73 秒**
- `FileStorageTests`：**7/7 通过**

---

## 3. 最终结论

1. 异常退出归因于 **WorkBuddy 沙箱或测试命令通道限制**，不是已证实的项目缺陷。
2. 不得因此修改 `FileStorage`、测试代码或生产代码。
3. 后续凡涉及真实文件 IO 的最终测试验收，应在普通 PowerShell 环境执行。
4. 诊断产生的 `s12-*.out`、构建日志、`TestResults`、diag、hang、dump 等仅属开发诊断材料，不应进入正式发布包。

---

## 4. 完成声明

- 本阶段生产代码：**零改动**。
- 本阶段测试代码：**零改动**。
- S1—S11 基线：已由用户普通 PowerShell 验收通过。
- S12.1：**完成并关闭**，不得自动进入或重跑。

