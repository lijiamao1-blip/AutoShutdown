# S14 结果记录（工作日复杂排程与一次性日期）

> 执行书：`S14-work包/AutoShutdown-V2-S14-阶段一键执行书.docx`
> 执行日期：2026-08-16
> 范围：S14 全阶段（检查点 1~4）。阶段完成后停止，未进入 S15。
> 前置：S13 已通过并冻结 TaskDefinition / TaskCollection / 调度扩展契约。

---

## 1. 阶段结论

**结论：通过。**

| 验收条件（执行书 §10） | 状态 |
|------|------|
| 工作日规则可从 UI 创建、保存、恢复并得到确定的下次触发 | ✅ C2 计算器 + C3 调度 + C4 UI 切片闭环 |
| 一次性日期可从 UI 创建、保存、恢复并得到确定的下次触发 | ✅ OneTime 映射 + 纯函数计算 + 持久化往返 |
| 无后续触发、过期和 DST 行为均有可重复证据 | ✅ 测试断言（OneTimeExpired / 跨午夜 / DST gap·overlap） |
| 自动化测试不触发真实电源操作 | ✅ 全部使用替身（Fake/Recording PowerService） |
| TreatWarningsAsErrors | ✅ Release build 0 警告 0 错误 |

---

## 2. 实施顺序（4 个检查点）

| 检查点 | 内容 | 提交 |
|------|------|------|
| C1 | schema V2 + V1→V2 身份迁移 + TaskDefinition/TaskKind 字段增量 | `9285138` |
| C2 | Weekdays/NextWorkday/NthWorkdayOfMonth/OneTime 纯函数实现 + 结构校验 | `d09408b` |
| C3 | 调度器消费统一计算接口 + 周期重排 + tasks.json 启动载入 + 过期一次性即终结 | `246c2a3` |
| C4 | UI 规则选择/日期时间/例外日历/错误提示 + 创建后持久化 | `c8d50fc` |

---

## 3. 交付物

### 3.1 核心（AutoShutdown.Core）

| 文件 | 职责 |
|------|------|
| `Tasks/TaskDefinition.cs` | 新增 `Weekdays` / `NthWorkday` / `OneTimeDateTime` / `HolidayDates` 可空字段 |
| `State/Enums.cs` | `TaskKind` 增 Weekdays/NextWorkday/NthWorkdayOfMonth/OneTime；`NextExecutionStatus` 增新错误结果 |
| `Storage/TasksDocument.cs` | `CurrentSchemaVersion` 1→2 |
| `Storage/TasksDocumentStore.cs` | V1→V2 身份迁移（原子写回留 .bak）；version>2→UnsupportedVersion、<1→Invalid；save 仅接受 V2 |
| `Tasks/TaskDefinitionValidator.cs` | 新规则结构校验 + `IsRecurringKind` 判定 |
| `Scheduling/NextExecutionCalculator.cs` | 统一纯函数：`Calculate(TaskDefinition, now, TimeZoneInfo)` |
| `Scheduling/NextExecutionResult.cs` | 统一错误结果（MissingWeekdays/InvalidNthWorkday/OneTimeExpired 等） |
| `Scheduling/SchedulerEngine.cs` | 启动载入 tasks.json；周期规则 executed→waiting 重排；过期一次性诞生即 Cancelled；损坏 SetFaulted |
| `Abstractions/ITaskService.cs` | 增 `RescheduleRecurring` |
| `Tasks/TaskService.cs` | `RescheduleRecurring` 实现 |

### 3.2 应用层（AutoShutdown.App）

| 文件 | 职责 |
|------|------|
| `Presentation/MainWindowViewModel.cs` | `TimeMode` 7 种；模式→TaskDefinition 字段映射；规则字段校验；创建后经 `LoadTasksAsync`/`SaveTasksAsync` 持久化 |
| `MainWindow.xaml` | 4 列模式单选 + DatePicker + 工作日复选框 + 第 N 工作日下拉 + 例外日历多行输入 + 表单错误提示 |

### 3.3 测试（AutoShutdown.Tests）

| 文件 | 职责 |
|------|------|
| `S14_NextExecutionRuleTests.cs`（新增） | 27 项纯函数契约：过去/已过点/跨年/月末/第N不存在/节假日/DST gap·overlap/非系统时区/过期 |
| `S14_Checkpoint4_CreateRuleTests.cs`（新增） | 14 项：7 模式映射、规则校验、可见性标志、创建持久化（含既有任务无损保留） |
| `TasksDocumentStoreTests.cs` | 版本兼容矩阵 + 复杂规则 JSON 往返 |
| `ConfigurationServiceTests.cs` | LoadTasksAsync/SaveTasksAsync 契约（NotFound/Corrupt/往返/分文件） |
| `CorePipelineIntegrationTests.cs` | J_DailyAt 跨午夜改为断言周期重排（executed→waiting + 次日触发） |

---

## 4. 测试与构建结果

```
$ dotnet test -c Release --filter "FullyQualifiedName~S14_NextExecutionRuleTests|FullyQualifiedName~S14_Checkpoint4_CreateRuleTests|FullyQualifiedName~TasksDocumentStoreTests"
  已通过! - 失败: 0，通过: 57，已跳过: 0，总计: 57
```

```
$ dotnet test -c Release
  已通过! - 失败: 0，通过: 618，已跳过: 0，总计: 618
```

```
$ dotnet build -c Release
  已成功生成。0 个警告，0 个错误
```

（S14 全阶段累计 618，全部通过。）

---

## 5. 提交清单

| 提交 | 说明 |
|------|------|
| `7f74211` | S14 起点（S13-T08/T09 结果记录） |
| `9285138` | S14-C1: schema V2 + V1→V2 migration + task model fields |
| `d09408b` | S14-C2: workday/one-time rules + validator |
| `246c2a3` | S14-C3: scheduler consumes unified calc + recurring reschedule + tasks.json load |
| `c8d50fc` | S14-C4: rule-selection UI slice + tasks.json persistence |

工作树：干净（仅余参考文档 `S13-work包/…docx`、`S14-work包/`，均非交付物，不纳入提交）。

---

## 6. 端到端验收证据

| 环节 | 证据 |
|------|------|
| 创建 | `S14_Checkpoint4_CreateRuleTests.TryBuildDefinition_*`（7 模式字段映射） |
| 保存 | `CreateCommand_PersistsDefinitionToTasksJson`（SaveTasksAsync 收到 V2 文档） |
| 重启恢复 | `TasksDocumentStoreTests.SaveAsync_WhenS14ComplexRules_WritesAndRoundTrips`（4 种新规则 JSON 往返，字段全保留）+ C3 `SchedulerEngine` 启动载入 tasks.json |
| 下次触发展示 | `S14_NextExecutionRuleTests` 27 项 + C3 周期重排（executed→waiting + 次日触发） |
| 一次性终结 | `OneTime_WhenPast_ReturnsOneTimeExpired` + C3 过期一次性诞生即 Cancelled |

---

## 7. 遗留 / 开放项（≤3，不阻断）

1. **真机电源未执行**：全阶段自动化测试均使用替身电源；真实关机/重启/休眠/休眠唤醒未在真机执行，需人工按隔离候选步骤验证（见 §9）。
2. **UI 仅 VM + XAML 切片**：无运行时截图/人工点击验收，`MainWindow.xaml` 绑定依赖 WPF 运行时解析，未做真机 UI 冒烟。
3. **`_s14_extracted.txt` 为临时提取文件**：已删除（见 §8），不纳入提交。

---

## 8. 禁止边界执行声明

- 未修改 `PowerRequest` / `PowerResult` / `GuardedPowerService` / `Win32PowerService` / `FakePowerService` / `ShutdownWorkflow` / 唯一电源出口；`ShutdownWorkflow` 仍是唯一调用 `IPowerService` 的模块（结构性测试锁定）。
- 自动化测试全部使用替身电源，未触发真实关机/重启/休眠。
- 不重写 S13 状态机、仲裁或调度循环所有权。
- 未触碰 S15 范围。
- 临时文件 `_s14_extracted.txt` 已删除，未提交。

---

## 9. 未执行 / 人工待验（真机项）

以下项目未执行（环境无安全授权/隔离真机），**明确标注为未执行**，不写为通过：

1. 真实电源操作冒烟（关机/重启/睡眠/休眠）。
2. 真机 WPF UI 手动冒烟与截图。
3. 开机自启动 / 托盘 / 注册表项真机验证。

---

## 10. 是否可进入 S15

**是。** S14 四检查点全部通过、Release 全量测试与构建干净、工作树干净、遗留项均不阻断。可按执行书 §12 将本回报包交独立验收（ChatGPT）后新开对话进入 S15。
