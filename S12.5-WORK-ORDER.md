# AutoShutdown S12.5 机械执行工作单（真实电源实现与双闸门）

> 状态：待执行
> 日期：2026-08-12
> 项目根目录：`D:\电脑定时关机重建完整版`
> 执行者定位：机械执行助手。严格按本工作单实施，不自行扩展设计，不承担架构重构或功能扩展。
> 前置条件：S12.3、S12.4、S12.4.1、S12.4.2、S12.4.3 已通过验收；当前为 `FakePowerService` 安全测试模式
> 本阶段结束后立即停止，不进入 S12.6，不进行真实电源人工灰度验收

---

## 0. 阶段目标

本阶段只实现"真实电源的唯一出口"与"双闸门"安全机制。**不改变任何真实系统行为，默认状态下真实电源仍然不可用。**

必须达到：

1. 新增唯一真实电源出口 `Win32PowerService`，全代码库只允许它调用 Windows 电源 API。
2. 禁止使用 `shutdown.exe`、PowerShell 命令、`Process.Start` 或任意进程启动方式执行电源操作。
3. 默认注入仍表现为 `FakePowerService`（安全测试模式）；只有同时满足双闸门才可能走真实电源。
4. 双闸门：
   - 闸门一（发布配置）：配置文件中显式开启 `RealPowerEnabled=true`；
   - 闸门二（人工确认）：用户在界面针对该次真实动作进行明确人工确认。
5. `TestMode=true` 时无条件走 `FakePowerService`，即使 `RealPowerEnabled` 被误设为 true。
6. 自动化测试全部通过替身完成，测试阶段**绝不**真的关机、重启、睡眠或休眠。
7. 检查全代码库不存在其他真实电源调用路径。

完成后立即停止，等待 GPT 审查；不得进入 S12.6 的人工灰度验收。

---

## 1. 开工前只读检查（必须先报告）

修改任何文件前，先只读检查并汇报：

1. `AutoShutdown.Core.Abstractions.IPowerService`、`PowerRequest`、`PowerResult`、`PowerOutcome` 的现有定义。
2. `AutoShutdown.Core.Power.FakePowerService` 的现状。
3. `ShutdownWorkflow.ExecuteAsync` 的完整闸门顺序，特别是 `TestModeRequired` 的判定位置。
4. `AppConfig`、`ConfigurationValidator`、`ConfigurationService` 的现有字段、默认值与校验逻辑。
5. `TaskInstance`、`TaskDefinition` 的现有字段（确认标记需要落在这里）。
6. `ServiceRegistration.cs` 中 `IPowerService` 的当前注册方式。
7. 以下既有测试中所有针对真实电源的断言（这些断言在 S12.5 新增实现后**必然需要同步更新**）：
   - `AppHostSourceContractTests.cs`
   - `S11UiSourceContractTests.cs`
   - `S12LoggingTests.cs`
   - `S12_3AutoStartTests.cs`
   - `S12_4_3IconContractTests.cs`
   - `SchedulerEngineTests.cs`
   具体包括：`Assert.DoesNotContain("Win32PowerService"/"DllImport"/"Process.Start"/"shutdown.exe")`、`Assert.IsType<FakePowerService>`、`Assert.Contains("AddSingleton<IPowerService, FakePowerService>")`。
8. 准备新增/修改的准确文件清单、测试清单和预计测试数量。

发现已存在第二套真实电源实现或既有测试与真实电源冲突时，立即停止并报告，不得自行绕过。

---

## 2. 强制安全边界（任何一项不满足都必须停止）

1. `TestMode=true` 时无条件走 `FakePowerService`；本阶段任何情况下都不得让 `TestMode=true` 触发真实电源。
2. 默认（新装、升级、恢复出厂配置）`RealPowerEnabled=false`、`TestMode=true`，真实电源不可用。
3. `Win32PowerService` 是**唯一**允许包含 Windows 电源 P/Invoke 的文件；全代码库其他 `.cs` 一律禁止 `DllImport`、`ExitWindowsEx`、`SetSuspendState`、`shutdown.exe`、`Process.Start` 电源用途。
4. 禁止 `shutdown.exe`、PowerShell 命令、`Process.Start`、`System.Diagnostics.Process` 或其他进程启动方式执行电源操作。
5. 真实电源调用必须发生在 `Win32PowerService` 内部，且该调用路径必须被可注入替身隔离，保证自动化测试零真实调用。
6. 双闸门任一未满足时，电源请求必须返回拒绝结果，**不得**有任何真实调用副作用。
7. 只允许修改：`AppConfig` 新增字段、`ConfigurationValidator`、`ShutdownWorkflow` 闸门、`PowerResult`/`ShutdownWorkflowResult` 相关枚举的兼容扩展、App 层电源基础设施、DI 注册、以及必须同步更新的既有测试断言。
8. 禁止修改：状态机合法转换表、调度器、`StageToken`/`InstanceId` 校验、工作流执行顺序之外的语义、配置存储与迁移、日志保留策略、托盘/自启动/发布逻辑。
9. 不得进入 S12.6，不得进行真实关机、重启、睡眠或休眠的人工验收。
10. 不得删除、移动或覆盖源码、测试、工作单、诊断证据或既有发布包。

---

## 3. 允许新增与修改的范围

### 3.1 新增文件（建议）

```
src/AutoShutdown.App/Infrastructure/Power/Win32PowerService.cs
src/AutoShutdown.App/Infrastructure/Power/IPowerNativeApi.cs      （P/Invoke 隔离接口）
src/AutoShutdown.App/Infrastructure/Power/Win32PowerNativeApi.cs （唯一含 DllImport 的实现）
src/AutoShutdown.App/Infrastructure/Power/GuardedPowerService.cs （双闸门 + 路由）
src/AutoShutdown.App/Infrastructure/Power/IPowerConfirmationService.cs
src/AutoShutdown.App/Infrastructure/Power/PowerConfirmationService.cs
tests/AutoShutdown.Tests/S12_5Win32PowerContractTests.cs
tests/AutoShutdown.Tests/S12_5PowerGateTests.cs
tests/AutoShutdown.Tests/S12_5WorkflowGateTests.cs
tests/AutoShutdown.Tests/S12_5ConfigValidationTests.cs
```

### 3.2 修改文件（建议）

- `src/AutoShutdown.Core/Configuration/AppConfig.cs`：新增 `bool RealPowerEnabled`（默认 false）。
- `src/AutoShutdown.Core/Configuration/ConfigurationValidator.cs`：新增 `RealPowerEnabled` 与 `TestMode` 互斥校验。
- `src/AutoShutdown.Core/Workflow/ShutdownWorkflow.cs`：调整测试模式闸门与真实电源闸门。
- `src/AutoShutdown.Core/Power/PowerResult.cs`：按需扩展（见 §6）。
- `src/AutoShutdown.Core/Workflow/ShutdownWorkflowResult.cs` 及相关枚举：按需增加 `Accepted` 状态与决策码。
- `src/AutoShutdown.Core/State/RuntimeState.cs`（`TaskInstance`）：新增真实电源确认标记（默认 false，向后兼容）。
- `src/AutoShutdown.App/AppHost/ServiceRegistration.cs`：注册 `Win32PowerService`、`GuardedPowerService` 与确认服务。
- `src/AutoShutdown.App/Presentation/MainWindowViewModel.cs`：真实电源模式下的创建前确认与确认标记传递。
- 以下既有测试仅更新"全源码禁止真实电源 / DI 断言"，不得弱化对 `FakePowerService` 默认行为的验证：
  - `AppHostSourceContractTests.cs`
  - `S11UiSourceContractTests.cs`
  - `S12LoggingTests.cs`
  - `S12_3AutoStartTests.cs`
  - `S12_4_3IconContractTests.cs`
  - `SchedulerEngineTests.cs`

---

## 4. 真实电源实现要求

### 4.1 `IPowerNativeApi`（隔离接口）

```csharp
public interface IPowerNativeApi
{
    // 返回 (成功?, 系统错误码?)
    (bool Succeeded, int? NativeErrorCode) Shutdown();
    (bool Succeeded, int? NativeErrorCode) Restart();
    (bool Succeeded, int? NativeErrorCode) Sleep();
    (bool Succeeded, int? NativeErrorCode) Hibernate();
}
```

### 4.2 `Win32PowerNativeApi`（唯一 DllImport 位置）

- 只允许在此文件内出现：
  - `ExitWindowsEx`（`user32.dll`，关机/重启）
  - `SetSuspendState`（`powrprof.dll`，睡眠/休眠）
  - `AdjustTokenPrivileges` / `OpenProcessToken` / `LookupPrivilegeValue` / `SetTokenInformation`（`advapi32.dll`，仅用于获取 `SE_SHUTDOWN_NAME` 权限）
  - 必要的 `DllImport` 声明
- 禁止在此文件内出现 `Process`、`shutdown.exe`、`PowerShell` 或字符串命令执行。
- 休眠若系统不支持，返回失败并带合理 `NativeErrorCode`。

### 4.3 `Win32PowerService`

- 实现 `AutoShutdown.Core.Abstractions.IPowerService`。
- 构造注入 `IPowerNativeApi`（生产传 `Win32PowerNativeApi`，测试传替身）。
- 将 `PowerRequest.Action` 映射到 `IPowerNativeApi` 对应方法：
  - `Shutdown`、`Restart` → 对应调用；
  - `Sleep`、`Hibernate` → 对应调用；
  - `Unknown` 或非法动作 → 返回 `PowerOutcome.Rejected`。
- 成功返回 `PowerOutcome.Accepted`，失败返回 `PowerOutcome.Failed` 并携带 `NativeErrorCode`。
- 该服务**本身不校验配置与确认**；闸门由 `GuardedPowerService` 负责（纵深防御仍要求工作流层面也有配置闸门）。

### 4.4 确认标记落点

- `TaskInstance` 新增字段（默认值保证向后兼容，无需迁移）：
  - `bool RealPowerConfirmed`（默认 false）
  - 可选 `Guid? RealPowerConfirmationId`
- `PowerRequest` 沿用 `InstanceId` 等字段；如需向电源服务传递确认状态，可新增只读字段 `bool RealPowerConfirmed`（默认 false），由工作流从 `TaskInstance` 拷贝。

---

## 5. 双闸门设计

### 5.1 闸门一：发布配置（配置层）

- `AppConfig` 新增 `bool RealPowerEnabled`，默认 `false`。
- `ConfigurationValidator` 新增互斥校验：
  - `RealPowerEnabled == true && TestMode == true` → 校验错误（`RealPowerEnabled` 与 `TestMode` 互斥），拒绝保存/加载。
- 工作流层：`ShutdownWorkflow` 中，当 `TestMode == false` 时：
  - `RealPowerEnabled == false` → `Reject(RealPowerNotEnabled)`，**不调用**任何电源服务；
  - `RealPowerEnabled == true` → 继续进入执行路径（仍待闸门二确认）。

### 5.2 闸门二：人工确认（UI/执行层）

- 创建真实电源任务时（`TestMode=false` 且 `RealPowerEnabled=true`），UI 必须弹出明确二次确认，文案至少说明：
  - 该任务将在倒计时结束后执行**真实**关机/重启/睡眠/休眠；
  - 请保存所有工作；该操作无法通过本软件在事后撤销。
- 用户点击"确认并创建"后，才在 `TaskInstance` 上写入 `RealPowerConfirmed=true`（及 `RealPowerConfirmationId`）。
- 用户取消：零写入、零任务创建。
- `GuardedPowerService.ExecuteAsync` 校验：
  - `TestMode == true` → 无条件调用 `FakePowerService`，返回 `Simulated`；
  - `RealPowerEnabled != true` → 返回 `Rejected`；
  - 请求/实例未携带 `RealPowerConfirmed == true` 或确认 ID 不匹配 → 返回 `Rejected`；
  - 全部通过 → 调用 `Win32PowerService`。
- 命令失败/被拒时，UI 必须显示稳定中文原因，不得静默。

### 5.3 路由注册

- `ServiceRegistration.cs`：
  - 保留 `FakePowerService` 作为显式单例；
  - 注册 `Win32PowerService` 与 `Win32PowerNativeApi`；
  - 将 `IPowerService` 注册为 `GuardedPowerService`（内部持有 Fake + Win32 + `IConfigurationService` + `IPowerConfirmationService`）。
- 效果：默认（`TestMode=true`、`RealPowerEnabled=false`）行为与现在完全一致，即 `IPowerService` 表现为模拟记录，绝不真实执行。

---

## 6. 结果类型与决策码扩展

按需对 Core 做**兼容扩展**：

1. `ShutdownWorkflowStatus` 增加 `Accepted`（真实电源成功），并保持既有枚举值不变。
2. `ShutdownWorkflowResult.Succeeded` 应同时认可 `Simulated` 与 `Accepted`（现有逻辑只认 `Simulated`）。
3. `ShutdownDecisionCode` 增加：
   - `RealPowerNotEnabled`
   - `RealPowerConfirmationMissing`
   保持既有枚举值不变。
4. 如不便于在 `PowerResult` 增加字段，可仅依靠 `PowerOutcome`（`Accepted`/`Rejected`/`Failed`/`Simulated`）与 `Message` 表达；但必须保证工作流能够区分"配置未启用"与"缺少确认"两种拒绝原因。

禁止修改既有枚举成员的数值或含义；新增成员一律追加到末尾。

---

## 7. 工作流闸门修改（核心）

`ShutdownWorkflow.ExecuteAsync` 现有逻辑中 `if (!config.TestMode) return Reject(TestModeRequired)` 必须调整，以支持真实电源：

1. `TestMode == true`：保持现有全部校验与模拟执行路径（行为不变）。
2. `TestMode == false`：
   - 先检查 `config.RealPowerEnabled`；为 false 时 `Reject(RealPowerNotEnabled)`；
   - 为 true 时，继续校验实例合法性（状态、身份、动作、计时等，顺序不变）；
   - 校验确认标记：`instance.RealPowerConfirmed != true` → `Reject(RealPowerConfirmationMissing)`；
   - 将 `PowerRequest.RealPowerConfirmed` 置为 `instance.RealPowerConfirmed` 后提交给 `_powerService`（此时为 `GuardedPowerService`）。
3. 其余决策码（`TestModeRequired` 仍保留用于旧逻辑或作为不应出现的兜底）不允许被删除，只允许改变触发条件或保留原义。

禁止改动：`InvalidState`、`ExecutionFlagMissing`、`InvalidIdentity`、`InvalidAction`、`ActionNotAllowed`、`InvalidTiming` 的判定顺序与含义。

---

## 8. UI 行为要求

1. 首页/创建区域在 `TestMode=false` 且 `RealPowerEnabled=true` 时，显示"真实电源模式"标识；创建任务前必须二次确认（见 §5.2）。
2. `TestMode=true`（默认）时，UI 文案保持"安全测试模式"，不显示真实电源确认流程。
3. 创建真实任务失败、配置未启用、缺少确认时，按钮禁用原因或错误信息必须具体可见，不得"点击无反应"。
4. 不改变首页布局、托盘菜单、提醒窗口、按钮启用条件之外的视觉结构。

---

## 9. 日志要求

新增以下低敏事件（不记录敏感参数）：

- `RealPowerRequested`（已确认，将调用真实电源）
- `RealPowerRejectedConfigNotEnabled`
- `RealPowerRejectedMissingConfirmation`
- `RealPowerSucceeded`
- `RealPowerFailed`

`TestMode=true` 的模拟路径继续使用既有事件名，不新增真实电源日志。

---

## 10. 测试要求

所有电源调用必须通过替身（`IPowerNativeApi` fake、`FakePowerService`、`GuardedPowerService` 注入 fake）完成；自动化测试**不得**真实调用 `ExitWindowsEx`/`SetSuspendState`。

### 10.1 新增测试

**`S12_5Win32PowerContractTests`**（静态契约 + 替身行为）
1. `Win32PowerNativeApi` 是唯一包含 `DllImport` 的源文件；
2. `Win32PowerNativeApi` 不含 `shutdown.exe`、`Process`、`PowerShell`；
3. 全源码除 `Win32PowerNativeApi` 外不存在 `ExitWindowsEx`、`SetSuspendState`、`DllImport`；
4. 全源码仍不存在 `shutdown.exe`、`Process.Start`（真实电源用途）；
5. `Win32PowerService` 正确映射四种动作并返回 `Accepted`/`Failed`（用 fake native api）；
6. `Unknown` 动作返回 `Rejected`。

**`S12_5PowerGateTests`**（双闸门）
1. `TestMode=true` 且 `RealPowerEnabled=true` 时，`GuardedPowerService` 走 Fake，零 Win32 调用；
2. `TestMode=false` 且 `RealPowerEnabled=false` 时返回 `Rejected`，零 Win32 调用；
3. `TestMode=false`、`RealPowerEnabled=true`、无确认时返回 `Rejected`，零 Win32 调用；
4. `TestMode=false`、`RealPowerEnabled=true`、确认通过时调用 Win32 替身并返回其结果；
5. 确认 ID 不匹配时返回 `Rejected`；
6. 配置加载失败时安全拒绝，不抛异常；
7. 默认配置下 `IPowerService` 解析为 `GuardedPowerService`，且在测试模式下行为等于模拟。

**`S12_5WorkflowGateTests`**
1. `TestMode=false` 且 `RealPowerEnabled=false` → `RealPowerNotEnabled`，电源服务零调用；
2. `TestMode=false`、`RealPowerEnabled=true`、实例未确认 → `RealPowerConfirmationMissing`，零调用；
3. `TestMode=false`、`RealPowerEnabled=true`、实例已确认 → 通过并返回 `Accepted`（走 fake 电源）；
4. `TestMode=true` 的既有模拟路径行为不变（全量回归由既有测试保证）。

**`S12_5ConfigValidationTests`**
1. `RealPowerEnabled=true` 且 `TestMode=true` → 校验失败，拒绝保存；
2. `RealPowerEnabled=false` 且 `TestMode=true` → 合法；
3. `RealPowerEnabled=true` 且 `TestMode=false` → 合法（配合其他字段校验）。

### 10.2 既有测试同步更新（必须，不能删除/跳过）

只放宽"全源码禁止真实电源"与 DI 断言，**不得**弱化默认走 Fake 的验证：

1. 所有 `Assert.DoesNotContain("Win32PowerService", ...)` 改为"允许 `Win32PowerService`/`Win32PowerNativeApi` 存在，但 `DllImport`/`ExitWindowsEx`/`SetSuspendState` 只能出现在 `Win32PowerNativeApi`"。
2. 所有 `Assert.IsType<FakePowerService>(provider.GetRequiredService<IPowerService>())` 改为断言解析为 `GuardedPowerService`，并额外断言在 `TestMode=true` 下执行结果 `WasSimulated=true`。
3. 所有 `Assert.Contains("AddSingleton<IPowerService, FakePowerService>", ...)` 改为断言 `GuardedPowerService` 被注册为 `IPowerService`，且 `FakePowerService`、`Win32PowerService`、`Win32PowerNativeApi` 均已注册。
4. 既有全源码扫描中的 `Assert.DoesNotContain("shutdown.exe"/"Process.Start")` 保持原样（真实电源仍禁止这些）。

---

## 11. 验证顺序

严格按顺序执行：

1. Release 构建：`dotnet build .\AutoShutdown.sln --configuration Release`，0 错误、0 警告。
2. 运行本阶段新增定向测试（S12_5 四组）。
3. 运行全部测试，报告实际总数、通过数、失败数、跳过数。
4. 静态安全扫描：全源码仅 `Win32PowerNativeApi` 含电源 DllImport；无 `shutdown.exe`、`Process.Start`。
5. 确认 `TestMode=true`、`RealPowerEnabled=false` 的默认配置下应用启动、创建任务、模拟执行行为与 S12.4.3 一致。
6. **不进入** S12.6；不执行任何真实关机/重启/睡眠/休眠。

WorkBuddy 环境如再次出现 testhost 通道限制，只记录环境现象；不得为制造通过而修改测试。最终全量测试由用户在普通 Windows PowerShell 运行。

---

## 12. 立即停止条件

出现任一情况立即停止，等待 GPT 审查：

1. 无法在不修改状态机、调度器或任务服务的前提下接入真实电源。
2. `TestMode=true` 存在任何触发真实电源的路径。
3. 双闸门任一缺失或可被绕过。
4. 自动化测试需要真实调用电源 API 才能通过。
5. 必须弱化既有安全测试（删除/跳过）才能通过。
6. 默认配置下 `IPowerService` 不再表现为模拟。
7. 全源码扫描无法证明"电源 DllImport 只在 `Win32PowerNativeApi`"。
8. Release 构建或全量测试失败且无法合理解释。

停止时只报告证据与最小修复建议，不扩大修改范围。

---

## 13. 最终汇报格式

完成后必须逐项汇报：

1. 新增/修改文件完整清单。
2. 真实电源唯一出口的实现位置与允许的 API 列表。
3. 双闸门的两个闸门分别落在哪一层、如何验证。
4. 默认状态（`TestMode=true`、`RealPowerEnabled=false`）零真实调用的证据。
5. 配置新增字段与互斥校验结果。
6. 工作流闸门修改前后差异。
7. 新增测试名称、数量与通过情况。
8. Release 构建结果（0 错误、0 警告）。
9. 全量测试实际结果；如未在普通 PowerShell 验收，明确标为待验收。
10. 既有测试同步更新清单。
11. 明确声明未执行任何真实电源操作。
12. 明确声明未进入 S12.6。

---

## 14. 完成判定

只有同时满足以下条件，S12.5 才可标记完成：

1. 全代码库唯一真实电源出口为 `Win32PowerService`，且调用被替身隔离。
2. 双闸门全部实现且默认关闭。
3. `TestMode=true` 无条件走 Fake，行为与 S12.4.3 一致。
4. Release 构建 0 错误、0 警告。
5. 全量测试失败 0、跳过 0（或已如实记录环境限制）。
6. 全源码扫描证明无其他真实电源调用路径。
7. 未执行真实关机、重启、睡眠或休眠。

完成后立即停止，等待 GPT 审查与用户确认；不进入 S12.6。
