# AutoShutdown S12.3 机械执行工作单（待执行版）

> 状态：**待执行**  
> 日期：2026-08-11  
> 执行者定位：WorkBuddy 机械执行模型。严格按本工作单实施，不自行扩展设计。  
> 前置条件：S12.1 已完成（278/278）；S12.2 已完成并复验（303/303）；S12.2.1 UI 预览与微调已完成。  
> 完成后必须停止，等待 GPT 审查；**不得进入 S12.4**。

---

## 0. 阶段目标

本阶段只实现“开机自启动”的安全基础设施、显式用户开关、状态检查和修复能力。

必须达到：

1. 默认关闭，软件首次运行、升级或启动时不得自行写注册表。
2. 只有用户在“软件设置”页主动开启并二次确认后，才允许为当前用户启用。
3. 用户可以随时关闭；关闭应具有幂等性。
4. 注册项损坏、指向旧路径或无法访问时，界面必须显示真实状态，不得冒充成功。
5. 日志记录结果但不得记录敏感参数；日志失败不得改变启用/关闭结果。
6. 本阶段仍使用 `FakePowerService`，不得触碰真实电源。

---

## 1. 开工前只读检查（必须先报告）

修改文件前先只读检查并汇报：

1. `ServiceRegistration.cs` 当前 DI 注册。
2. `MainWindow.xaml`、`MainWindowViewModel.cs` 中“软件设置”占位页的现状。
3. `ApplicationLifetimeCoordinator`、`App.xaml.cs` 的启动和退出顺序。
4. 当前可执行文件路径的取得方式；禁止假定固定 D 盘路径。
5. 全仓库是否已有注册表、自启动、Startup 文件夹或计划任务相关代码。
6. 准备新增/修改的准确文件清单、测试清单和预计测试数量。

发现已有同类实现时，不得并存第二套机制；立即停止并报告。

---

## 2. 硬性安全边界

1. 只允许使用当前用户范围 `HKCU`；禁止 `HKLM`，禁止管理员权限。
2. 禁止 Startup 文件夹、任务计划程序、服务、计划任务、PowerShell 常驻脚本等替代机制。
3. 唯一允许的外部状态是当前用户 Run 注册项：
   `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`。
4. 注册项名称固定且唯一，例如 `AutoShutdown.Desktop`；不得覆盖其他软件项。
5. 默认不开启；DI 注册、应用启动、构造函数、读取状态均不得产生写操作。
6. 开启前必须由用户明确点击并确认；自动化测试不得触碰真实注册表。
7. 禁止修改 Core、调度器、状态机、任务服务、工作流、电源接口及其语义。
8. `IPowerService` 必须继续解析为 `FakePowerService`。
9. 禁止 P/Invoke、`shutdown.exe`、真实电源、claim、lease、Revision、第二调度循环。
10. 不实现安装器、发布包或真实电源灰度；这些属于后续独立阶段。

---

## 3. 建议接口与结果模型

在 App 基础设施层建立最小抽象，不放入 Core：

- `IAutoStartService`
  - `GetStatus()`：只读检查。
  - `Enable()`：显式启用。
  - `Disable()`：显式关闭。
  - `Repair()`：仅在用户主动点击修复时，将本软件已有但错误的项修正为当前路径。
- `AutoStartStatus` 至少区分：
  - `Disabled`
  - `Enabled`
  - `PathMismatch`
  - `InvalidValue`
  - `Unavailable`
- `AutoStartOperationResult` 至少包含：
  - 成功与否
  - 稳定的结果码
  - 中文可显示消息
  - 操作后的状态

所有失败应返回结果，不以未处理异常打断应用。参数或内部异常不得伪装为成功。

---

## 4. 路径和值生成规则

1. 路径必须取当前应用实际可执行文件路径，不得硬编码开发目录、用户名或盘符。
2. 写入值必须完整引用 EXE 路径：`"<绝对路径>"`。
3. 路径不存在、不是 `.exe`、无法取得或包含无法安全表达的内容时，默认拒绝启用。
4. 状态比较必须正确处理引号、大小写和规范化绝对路径。
5. 只允许识别和修改固定注册项名称；不得枚举后批量清理其他项。
6. `Disable()` 在键或值不存在时仍返回成功（已关闭），保证幂等。
7. `Enable()` 已正确启用时返回成功（已启用），不得重复制造副作用。
8. 项存在但路径不一致时，读取状态返回 `PathMismatch`；不得在启动时自动覆盖。

---

## 5. UI 行为

将“软件设置”占位页改为最小可用页面，仅加入“开机自启动”卡片：

1. 显示当前状态：未启用、已启用、路径异常、不可用。
2. 提供“启用开机自启动”开关或按钮。
3. 开启前显示二次确认：说明仅为当前 Windows 用户启动本程序，可随时关闭。
4. 提供“关闭开机自启动”。
5. 仅在 `PathMismatch`/`InvalidValue` 时显示“修复自启动”。
6. 操作期间禁用重复点击；完成后重新读取真实状态。
7. 失败时显示具体原因，不得只显示“操作失败”。
8. 不增加“立即关机”、管理员运行、真实电源或其他高级功能。
9. 不改变首页、提醒窗口、托盘菜单和现有 UI 视觉结构。

---

## 6. 日志要求

仅记录以下低敏事件：

- `AutoStartStatusChecked`
- `AutoStartEnableSucceeded/Failed`
- `AutoStartDisableSucceeded/Failed`
- `AutoStartRepairSucceeded/Failed`

不得记录完整注册表内容、命令行参数或用户目录的完整明文路径。日志失败不得影响操作结果。

---

## 7. 测试要求

测试必须通过替身或可注入的注册表存储抽象执行，**不得读写用户真实注册表**。

至少覆盖：

1. 默认状态只读且零写入。
2. 不存在时返回 Disabled。
3. 正确值返回 Enabled。
4. 旧路径返回 PathMismatch 且不自动修复。
5. 无效值返回 InvalidValue。
6. Enable 写入一次且路径带完整引号。
7. Enable 已启用时幂等、无重复写入。
8. Disable 删除固定项一次。
9. Disable 不存在时幂等成功。
10. Repair 仅修改本软件固定项。
11. 路径缺失/非 EXE 时拒绝启用且零写入。
12. 存储读取、写入、删除异常均安全返回失败结果。
13. UI 初始读取状态不写入。
14. 用户取消确认时零写入。
15. 操作完成后重新读取状态。
16. DI 可解析唯一 `IAutoStartService`。
17. `IPowerService` 仍为 `FakePowerService`。
18. 全源码继续禁止 `Win32PowerService`、`shutdown.exe`、真实电源调用。

不得删除、跳过或放宽现有 303 项测试。

---

## 8. 验证顺序

1. 执行 Release 构建，必须 0 错误、0 警告。
2. WorkBuddy 环境若再次出现 testhost 通道限制，只记录环境现象，不修改生产代码规避。
3. 用户在普通 PowerShell 执行全部测试，必须包含原 303 项和本阶段新增测试，失败 0、跳过 0。
4. 仅在用户明确同意人工验收时，才允许在当前用户注册表进行一次：状态检查 → 开启 → 再检查 → 关闭 → 再检查。
5. 人工验收结束必须恢复为“关闭”，不得遗留自启动项。
6. 本阶段不得为了验收重启电脑。

---

## 9. 完成汇报格式

最终只汇报：

1. 新增/修改文件清单。
2. 注册表位置、固定项名称与路径生成规则。
3. 默认关闭和显式确认的证据。
4. 开启、关闭、修复的行为与幂等性。
5. UI 页面变化。
6. 新增测试名称和数量。
7. Release 构建结果。
8. 全量测试结果；若未在普通 PowerShell 验收，必须明确标为待验收。
9. 是否接触真实注册表；如接触，最终是否恢复关闭。
10. 明确声明 `FakePowerService` 未改变、未执行真实电源。
11. 明确声明未进入 S12.4。

完成后立即停止，等待 GPT 审查。
