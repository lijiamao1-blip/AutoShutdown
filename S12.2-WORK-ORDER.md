# AutoShutdown S12.2 机械执行工作单（归档版）

> 状态：**已完成**  
> 完成日期：2026-08-11  
> 用途：记录应用日志模块的落地、缺陷修复及最终验收。**除非 GPT 明确重新开放，本工作单不得再次执行。**

---

## 0. 阶段目标与范围

### 0.1 阶段目标

为应用层建立安全、可诊断、可轮换的文件日志，并保证日志故障不会改变调度命令结果、不会中断应用启动、不会触碰真实电源安全边界。

### 0.2 允许范围

- 日志接口、日志条目、级别、文件写入、保留策略。
- `ShutdownWorkflow` 的日志装饰器及 App 组合根注册。
- 日志专项测试及因 DI 装饰器结构变化而过期的源码契约测试。

### 0.3 禁止范围

1. 不修改 Core 的调度、状态机、任务服务和工作流公开语义。
2. 不增加 `Win32PowerService`，不调用 P/Invoke、`shutdown.exe` 或 `Process.Start` 执行电源操作。
3. `IPowerService` 必须继续解析为 `FakePowerService`。
4. 不实现自启动、发布打包、真实电源灰度或 S12.3 内容。
5. 日志失败不得向业务层抛出异常，不得改变原命令结果。
6. 不记录完整配置 JSON、敏感信息或任意脚本内容。

---

## 1. 实际日志模块文件

日志模块位于：

`src\AutoShutdown.App\Infrastructure\Logging\`

- `IApplicationLogger.cs`
- `ApplicationLogEntry.cs`
- `ApplicationLogLevel.cs`
- `FileApplicationLogger.cs`
- `LogRetentionService.cs`
- `LoggingShutdownWorkflowDecorator.cs`

专项测试：

- `tests\AutoShutdown.Tests\S12LoggingTests.cs`

---

## 2. 必须满足的日志行为

1. 日志采用 UTF-8 文件写入，每条事件包含时间、级别、事件名和消息。
2. EventName、Message、异常消息及堆栈中的换行符必须转义，保证一条事件只占一条物理日志行。
3. 文件不可写、目录无效、时钟异常、轮换失败或 Flush 失败时静默降级，不影响业务结果。
4. 并发写入不得产生相互穿插的日志行。
5. 默认最低级别不写 Debug。
6. 保留策略只处理命名符合 `autoshutdown-YYYY-MM-DD[.N].log` 的日志文件；无关文件一律保留。
7. 保留最近日志并删除超过约定保留期的匹配文件；任意单文件删除失败不得阻断其他文件检查。
8. `ShutdownWorkflow` 通过 `LoggingShutdownWorkflowDecorator` 记录必要事件，内部仍调用原 `ShutdownWorkflow`，不得改变返回结果或异常语义。

---

## 3. 复核发现的三个缺陷及修复

### 3.1 保留任务异常泄漏

原问题：`Directory.Exists`、延迟枚举、时钟获取、无效路径等异常可能发生在删除操作的局部 try/catch 之外并影响启动。

修复：`RunOnce()` 使用整体保护，并为每个文件单独保护；单个文件失败跳过，删除范围不扩大。

### 3.2 日志时间源异常泄漏

原问题：`_now()` 和日志条目创建位于保护范围之外，时间源抛错会泄漏到业务层。

修复：时间获取、条目创建、格式化、目录创建、轮换、写入和 Flush 全部纳入安全保护。

### 3.3 多行日志产生孤立行

原问题：消息和异常堆栈中的换行会生成缺少时间、级别和事件名前缀的孤立行。

修复：统一消毒 `\r`、`\n`，异常堆栈限制并连接后再消毒，确保每条事件为单行。

---

## 4. 新增回归测试

以下五项为缺陷修复新增测试：

1. `Log_WhenClockThrows_DoesNotThrow`
2. `Format_NewlinesInEventAndMessage_ProduceSingleLine`
3. `ExceptionStackTrace_DoesNotProduceOrphanLines`
4. `RunOnce_WhenDirectoryPathInvalid_DoesNotThrow`
5. `RunOnce_WhenClockThrows_DoesNotThrow_AndLaterCallsWork`

原有轮换、UTF-8、保留期、无关文件、并发写入、日志失败不改变结果等测试不得删除或弱化。

---

## 5. DI 契约测试修正

原 S8 测试曾以固定源码字符串断言 `IShutdownWorkflow` 直接注册到 `ShutdownWorkflow`。S12.2 合法引入装饰器后，该断言过期。

只允许更新测试以验证真实安全结构：

- `IPowerService` 解析为 `FakePowerService`。
- `ShutdownWorkflow` 本体为 Singleton。
- `IShutdownWorkflow` 解析为 `LoggingShutdownWorkflowDecorator`。
- 装饰器内部使用真实 `ShutdownWorkflow` 和 `IApplicationLogger`。
- `IScheduledTaskHandler` 仍为 `ShutdownScheduledTaskHandler`。
- 全源码仍不得出现 `Win32PowerService`、`shutdown.exe`、`Process.Start`、`DllImport`。

不得修改生产 DI 来迎合旧字符串断言。

---

## 6. 最终验收结果

用户在普通 Windows PowerShell 中执行 Release 全量测试：

- 测试总数：**303**
- 通过：**303**
- 失败：**0**
- 跳过：**0**
- 总耗时：约 **2.43 秒**
- Release 构建：**3 项目，0 错误，0 警告**

---

## 7. 完成声明

- S12.2 日志模块：**完成并关闭**。
- 最终安全边界：`FakePowerService` 未改变。
- 未进入真实电源、自启动或发布打包。
- 后续不得重复执行本工作单；如需调整日志，必须另建独立工作单。

