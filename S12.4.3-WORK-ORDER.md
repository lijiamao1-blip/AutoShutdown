# AutoShutdown S12.4.3 机械执行工作单（图标生成、接入与验收）

> 状态：**已完成**
> 日期：2026-08-12
> 完成验收日期：2026-08-13
> 项目根目录：`D:\电脑定时关机重建完整版`
> 执行者定位：机械执行助手，只做图标资产生成与图标接入，不承担架构重构或功能扩展。
> 前置条件：S12.4.2 已完成诊断与修复并等待审查；当前为 `FakePowerService` 安全测试模式
> 本阶段结束后立即停止，不进入 S12.5，不启用真实电源

---

## 0. 阶段目标

将现有原创 `assets\icon.svg` 转换为 Windows 多尺寸图标，并统一接入以下位置：

1. EXE 文件图标；
2. 主窗口左上角标题栏图标；
3. Windows 任务栏图标；
4. 系统托盘图标；
5. 关机前提醒窗口左上角图标。

本轮仅处理图标资产和图标接入，不修改 UI 布局、业务逻辑、状态机、调度器、配置、日志、自启动或电源实现。

---

## 1. 开工前只读检查（必须先报告）

修改前先检查并报告：

1. `AutoShutdown.App.csproj` 当前图标相关配置；
2. `App.xaml`、`MainWindow.xaml`、`ReminderWindow.xaml` 的窗口图标设置；
3. `TrayIconService.cs` 当前托盘图标来源、加载方式和释放方式；
4. 发布脚本如何复制图标资源；
5. `assets\icon.svg` 是否存在、是否为透明背景、是否包含文字、水印、外部图片或网络引用；
6. 准备新增和修改的文件清单。

只读检查完成后直接实施，不等待批准；若发现必须修改 Core 或后台架构，立即停止并报告。

---

## 2. 资产生成要求

只允许使用现有原创 `assets\icon.svg`，不得使用此前带水印的 JPG，不得抠图或修复水印。

生成：

- `assets\icon.ico`
- `assets\icon.png`
- `assets\icon_16x16.png`
- `assets\icon_24x24.png`
- `assets\icon_32x32.png`
- `assets\icon_48x48.png`
- `assets\icon_64x64.png`
- `assets\icon_128x128.png`
- `assets\icon_256x256.png`

`icon.ico` 必须真实包含：

`16×16、24×24、32×32、48×48、64×64、128×128、256×256`

要求：

1. 全部为真正透明背景；
2. 无文字、无水印、无白底、无灰边、无阴影粘连；
3. 保持蓝紫渐变；
4. 16×16 和 24×24 优先保证轮廓清晰，必要时根据 SVG 几何单独加粗，不直接模糊缩放；
5. 不安装永久性的第三方项目依赖；需要转换工具时优先使用现有本地工具，并说明工具名称。

---

## 3. 项目接入

根据实际项目结构完成接入：

1. 在 App 项目中设置 `ApplicationIcon`，确保生成的 EXE 自带 `icon.ico`；
2. 主窗口和提醒窗口使用同一图标资源；
3. `TrayIconService` 使用同一份多尺寸 ICO；
4. 托盘图标加载后必须保证生命周期有效，不得因流提前释放而变成空白图标；
5. 应用退出时正确释放托盘图标资源；
6. 发布时图标必须被嵌入或随包复制，不能依赖开发目录绝对路径；
7. 不改变托盘菜单内容、单实例逻辑、窗口关闭行为和 Named Pipe 行为。

---

## 4. 测试与验收

### 4.1 新增图标契约测试

至少验证：

1. App 项目已配置 `ApplicationIcon`；
2. ICO 文件存在且包含规定尺寸；
3. PNG 均有 Alpha 通道；
4. 图标不存在全白背景；
5. 主窗口和提醒窗口已接入图标；
6. 托盘服务使用正式图标，不再使用系统默认占位图；
7. 发布目录包含或内嵌正确图标；
8. `IPowerService` 仍解析为 `FakePowerService`；
9. 不含 `Win32PowerService`、`shutdown.exe`、`Process.Start` 或新增 P/Invoke 电源调用。

### 4.2 执行顺序

随后执行：

1. Release 构建；
2. 全部现有测试；
3. 如 WorkBuddy 测试通道失联，只记录环境限制，并给出普通 PowerShell 验收命令，不得修改代码规避；
4. 只有构建和测试通过后，才重新生成安全测试版发布包。

测试总数按实际发现结果如实报告，不预设固定数量。

---

## 5. 人工视觉验收清单

生成最新版后，由用户检查：

- 标题栏图标；
- 任务栏图标；
- 系统托盘图标；
- 提醒窗口图标；
- Windows 浅色与深色任务栏；
- 100%、125%、150%、200% 缩放；
- 16×16、24×24、32×32 下无白边、灰边、模糊和裁切。

如果 Windows 仍显示旧图标，先确认运行的是最新 EXE；说明可能存在系统图标缓存，不得擅自删除系统缓存或重启资源管理器。

---

## 6. 安全限制

- 不修改 Core；
- 不修改 ViewModel 和业务绑定；
- 不修改状态机、StageToken、调度器或工作流；
- 不写注册表，不改变自启动状态；
- 不执行真实关机、重启、睡眠或休眠；
- `FakePowerService` 必须保持不变；
- 不删除源码、开发环境或诊断证据；
- 不进入 S12.5。

---

## 7. 立即停止条件

出现任一情况立即停止，不得继续：

1. 必须修改 Core、状态机、调度器或工作流才能完成接入；
2. `IPowerService` 不再是 `FakePowerService`；
3. 发现真实电源调用路径或新增电源 P/Invoke；
4. 只能使用带水印 JPG、抠图或修复水印才能得到图标；
5. 图标资源依赖开发目录绝对路径才能加载；
6. Release 构建或测试失败且无法合理解释；
7. 需要删除系统图标缓存或重启资源管理器才能验收。

停止后只报告证据和最小修复建议，等待 GPT 审查。

---

## 8. 最终汇报

完成后只汇报：

1. 新增和修改的文件；
2. ICO 实际包含的尺寸；
3. SVG、PNG、ICO 的透明背景验证结果；
4. EXE、窗口、任务栏、托盘和提醒窗口的接入位置；
5. Release 构建结果；
6. 新增测试名称及数量；
7. 全量测试结果；
8. 最新发布目录和 EXE 的绝对路径；
9. 明确声明未修改 Core、真实电源与注册表；
10. 等待用户截图验收，不进入 S12.5。

---

## 9. 完成标准

只有同时满足以下条件，S12.4.3 才可标记完成：

1. 全部图标资产按规格生成且透明背景验证通过；
2. EXE、窗口、任务栏、托盘和提醒窗口均接入统一图标；
3. 托盘图标生命周期有效，退出时正确释放；
4. Release 构建 0 错误、0 警告；
5. 全量测试失败 0、跳过 0（或已如实记录环境限制）；
6. 发布包包含或内嵌正确图标；
7. `IPowerService` 仍为 `FakePowerService`，未执行真实电源操作；
8. 未修改 Core、注册表与自启动状态。

完成后立即停止，等待用户截图验收与 GPT 审查。

---

## 10. 完成验收汇报（2026-08-13）

> 本工作单已完成，以下为最终验收记录。

### 10.1 验收结果总览

| 验收项 | 结果 |
|---|---|
| Release 构建 | 0 错误、0 警告 ✅ |
| 全量测试 | **386/386 通过**，失败 0、跳过 0 ✅ |
| ICO 图标尺寸 | 16/24/32/48/64/128/256，共 7 帧，均 32bpp ✅ |
| PNG 透明背景 | 8 个 PNG 均带 Alpha 通道（colortype=6）✅ |
| EXE 内嵌图标 | 实测含 RT_GROUP_ICON，帧数=7 ✅ |
| 窗口/托盘/提醒接入 | 主窗口、托盘、提醒窗口共用 `/assets/icon.ico` ✅ |
| 导航栏 UI 调整 | 图标左移、文字放大、随窗口缩放、项间距、纵向位置按需完成 ✅ |
| 底部状态区域 | 任务状态/调度服务/配置状态/最近活动不再截断 ✅ |
| 发布包 | 两个 ZIP 重新生成，SHA-256 与清单一致，包内干净 ✅ |
| 安全边界 | `IPowerService` 仍为 `FakePowerService`，未执行真实电源 ✅ |

### 10.2 本轮代码改动清单

1. `tests/AutoShutdown.Tests/S12LoggingTests.cs`：修复日志文件名时区 bug（测试中 `Today` 由 `DateTime.UtcNow` 改为 `DateTimeOffset.Now`，与 `FileApplicationLogger` 本地时间命名一致）。
2. `src/AutoShutdown.App/MainWindow.xaml`：导航栏布局调整（图标对齐 `LineHeight`/`Margin`、文字 `FontSize 14→17`、导航列宽改为比例 `0.204*`、内容套 `Viewbox` 固定 `220×560`、导航项纵向间距与位置微调）。
3. `src/AutoShutdown.App/Themes/Controls.xaml`：导航项 `Margin "8,2"→"8,5"`（项间距 4px→10px）。

### 10.3 发布包信息

- 轻量版（framework-dependent）：`artifacts\release\v1.0.0\packages\AutoShutdown-v1.0.0-win-x64-framework-dependent.zip`
  - 大小：310,316 字节；SHA-256：`1D0EA5CFDD1258C55EDEA31AB33102507CB16FCC110575D57A9CFD2369C10D35`
- 免运行库版（self-contained）：`artifacts\release\v1.0.0\packages\AutoShutdown-v1.0.0-win-x64-self-contained.zip`
  - 大小：70,055,176 字节；SHA-256：`FA6A7249A49E4AABB2DAE098E617E0FB6C68550315AB1E7BC6594DB458412E4D`
- SHA-256 清单：`artifacts\release\v1.0.0\manifests\SHA256SUMS.txt`

### 10.4 完成标准核对

1. 图标资产按规格生成、透明背景验证通过 ✅
2. EXE/窗口/任务栏/托盘/提醒窗口统一图标 ✅
3. 托盘图标生命周期有效、退出释放 ✅
4. Release 构建 0 错误 0 警告 ✅
5. 全量测试失败 0、跳过 0 ✅
6. 发布包内嵌正确图标 ✅
7. `IPowerService` 仍为 `FakePowerService` ✅
8. 未修改 Core、注册表与自启动状态 ✅

**S12.4.3 标记完成。未进入 S12.5，未启用真实电源。**
