# AutoShutdown V2 — S18 阶段结果记录（关闭应用 / CloseApps）

## 阶段结论

**通过。**

S18 在 S17 完成的 Pre-Pipeline 框架上实现了 `CloseAppsAction`——按配置精确匹配目标进程（可执行路径 / 进程 ID，二者恰取其一），先优雅关闭（WM_CLOSE）、有界等待退出，仅在逐目标显式授权（`ForceKillAllowed`）时才强杀，且强杀前复核 PID + 启动时间防止 PID 重用误杀。四个检查点（C1 网关 / C2 服务与策略 / C3 Action 集成 / C4 真机验收）各自独立提交。Release 全量测试 823 通过、Release 构建 0 警告 0 错误。真机 Notepad 验收已编码为门控用例（默认不触发真实进程，需 `S18_REALMACHINE=1` 人工执行）。未提前进入后续阶段（RunCommands 仍属后续阶段，本阶段仅实现 CloseApps）。

## S18-D1 返修（安全加固 + 强杀授权 UI）

对 S18 独立评审确认的 4 处缺陷 + 1 项 UI 交付要求做定向返修。仅修这 4 处 + UI，不进入 S19、不扩范围、不重构无关代码。

### 缺陷与修复

| 缺陷 | 根因 | 修复 | 回归测试 |
|------|------|------|----------|
| D1-1 取消仍可能强杀 | `WaitForExit` 一次性阻塞等待，等待返回后、强杀前无取消复核 | 可取消的有界轮询等待（100ms 轮询 + 截止时间）；强杀前 `ThrowIfCancellationRequested` 复核；取消以 OCE 传播；取消路径绝不 `ForceKill`、绝不电源 | `CancellationDuringGracefulWait_PropagatesOce_AndNeverForceKills`；`CancellationArrivesDuringWait_BeforeForceKill_PropagatesOce_AndNeverForceKills`（均断言 `ForceKillCalls` 为空） |
| D1-2 未知会话放行 | `SessionId != CurrentSessionId` 把 `-1`（未知）当「非当前会话」放行 | 未知会话（`SessionId == -1`）一律 `SkippedProtected` fail-closed；仅 `SessionId == CurrentSessionId` 放行 | `UnknownSessionId_IsSkippedProtected_WithoutAnyClose`（断言 RequestClose/WaitForExit/ForceKill 全 0） |
| D1-3 未知退出伪装已退出 | `HasExited` 查询异常被空 catch 吞成 `true`，把「无法确认」当「已退出」 | 三态 `ProcessExitStatus`（Unknown/Exited/Running）；查询异常返回 Unknown；绝不 `AlreadyExited`、绝不强杀，fail-closed | `ExitStatusUnknown_IsFailure_NotAlreadyExited_NoForceKill`；`ExitStatusUnknown_BlocksPipeline_AndPowerZero`（e2e power=0） |
| D1-4 未确认强杀当成功 | `Kill` 后 `WaitForExit(1000)` 返回值未检查，进程仍存活也返回 `Killed` | 强杀后确认退出（有界 3000ms）；未确认或确认查询异常一律 `ExitNotConfirmed`，绝不 `Killed` | `ForceKill_ExitNotConfirmed_IsFailure_NotForceKilled`；`ForceKillExitNotConfirmed_BlocksPipeline_AndPowerZero`（e2e power=0） |

### UI 交付要求（执行书条款）

执行书第 3 节「已补充的阶段决策」第 26 行：「默认只优雅关闭；强杀必须逐目标显式 opt-in，并在 UI 显示风险。」该行位于「执行时直接采用」小节，无延期条款 → 本返修必须实现 UI（非延期）。

- 新增 `CloseAppsTargetRow`：强杀授权默认关闭；false→true 需经确认回调（测试注入替身，真机 MessageBox 风险提示），拒绝即回滚；授权后 `RiskVisible` 为真供 UI 显示风险。
- `MainWindowViewModel` 增加关闭应用设置区（目标增删、逐目标强杀勾选 + 风险提示、保存）；`MainWindow.xaml` 设置页新增「关闭应用（关机前）」卡片。
- 绝不自动启用强杀：缺失/非法配置 → 目标清空（fail-closed）；新增目标一律 `ForceKillAllowed=false`；仅显式 opt-in 或加载既有已授权配置才为真。

### 新增测试合计（21 项）

- 服务回归 5 项（`S18_CloseAppsServiceTests` D1 段）
- e2e 安全 3 项（`S18_D1_CloseAppsSafetyE2ETests`，均断言 `PrePipelineBlocked` + power=0）
- UI 13 项（`S18_D1_CloseAppsUiTests`：强杀 opt-in 拒绝/确认/关闭不询问、配置加载默认关闭、缺失配置空目标、配置不可用清空、按 pid/路径添加、重复/空输入报错、移除、保存逐目标授权持久化、路径/pid 恰取其一）

## S18-D2 返修（优雅等待结果三态）

对 S18 独立评审确认的 1 处缺陷做最小返修。仅修这一处，不进入 S19、不重做 D1、不扩范围。

### 缺陷与修复

| 缺陷 | 根因 | 修复 | 回归测试 |
|------|------|------|----------|
| D2 等待异常后「无法确认」折叠成普通超时并强杀 | `WaitForExit` 返回 `bool`，catch 块 `return GetExitStatus(pid) == Exited;` 把「确定仍在运行」与「退出状态无法确认（Unknown）」两类都折叠成一个 `false`，服务据此当作普通超时 → 若 `ForceKillAllowed` 则继续强杀 | 布尔改为三态 `ProcessWaitResult`（Unknown=0 / Exited=1 / TimedOut=2）；等待异常后复核退出状态：Exited→Exited、Unknown→Unknown、其余→TimedOut。服务处理：Exited→`ClosedGracefully`；Unknown→`ExitStatusUnknown`（绝不强杀、Action 默认 Block、power=0）；TimedOut（授权）→进入强杀路径、（未授权）→`TimedOut`；取消→OCE 原样传播（绝不折叠为 Unknown/TimedOut，强杀=0） | D2-1 `WaitExitUnknown_IsExitStatusUnknown_NotTimedOut_NoForceKill`；D2-2 `WaitExitUnknown_BlocksPipeline_AndPowerZero`（e2e power=0）；D2-3 `NormalTimeout_WithoutForceKill_TimedOut_NoForceKill` + `NormalTimeout_WithForceKill_EntersForceKillPath`；D2-4 `CancellationDuringWait_PropagatesOce_NeverReturnsTimeoutOrUnknown` + `CancellationDuringWait_PropagatesOce_AndPowerZero`（e2e power=0） |

### 新增测试合计（6 项）

- 服务回归 4 项（`S18_CloseAppsServiceTests` D2 段：D2-1、D2-3a、D2-3b、D2-4）
- e2e 安全 2 项（`S18_D1_CloseAppsSafetyE2ETests` D2 段：D2-2、D2-4，分别断言 `PrePipelineBlocked`+power=0 与 OCE+power=0）

### 关键改动文件

- `src/AutoShutdown.Core/CloseApps/ProcessWaitResult.cs`（新增）— 三态等待结果枚举。
- `src/AutoShutdown.Core/Abstractions/IAppWindowManager.cs`（改）— `WaitForExit` 返回 `ProcessWaitResult`。
- `src/AutoShutdown.App/Infrastructure/CloseApps/DiagnosticAppWindowManager.cs`（改）— 等待异常后复核三态映射。
- `src/AutoShutdown.Core/CloseApps/CloseAppsService.cs`（改）— `Unknown`/`TimedOut`/`Exited`/取消四分支处理。

## 提交清单

| 提交 | 内容 |
|------|------|
| 起始（承接点） | `78c97b8` S17 结果记录（含 D5 修复） |
| `31d7fcf` | S18-C1：Win32/进程网关边界（IProcessManager / IAppWindowManager 抽象 + 托管实现 + 源码契约测试） |
| `60effb2` | S18-C2：CloseApps 配置段 + 清单校验 + 编排服务 + 安全策略 + 服务/配置测试 |
| `38dcafa` | S18-C3：CloseAppsAction（默认 Block）接入组合根（置于流水线末尾）+ 动作/组合根测试 |
| `fc8484f` | S18-C4：真机验收门控测试（Notepad 专属实例优雅关闭，默认 no-op） |
| `44ee739` | S18 结果记录：CloseApps 阶段完成（C1-C4，823 全绿） |
| `69b4dfd` | S18-D1：关闭应用安全加固（取消/未知会话/未知退出/未确认强杀 fail-closed）+ 强杀授权 UI + 21 项回归/e2e/UI 测试 |
| `eb18a1a` | S18-D2：优雅等待结果三态（`ProcessWaitResult`，等待异常后 Unknown 不再折叠成普通超时/强杀）+ 6 项回归/e2e 测试 |
| 本提交 | S18-D2 结果记录：追加返修结论与证据 |
| 结束 | `eb18a1a`（+本提交） |

## 实际修改文件及职责

**新增（Core 抽象）**
- `src/AutoShutdown.Core/Abstractions/IProcessManager.cs` — 进程枚举/身份读取边界（只依赖抽象，绝不触碰真实 `Process`）。
- `src/AutoShutdown.Core/Abstractions/IAppWindowManager.cs` — 窗口/进程关闭边界（优雅 `CloseMainWindow` + 强杀前 PID/启动时间复核）。
- `src/AutoShutdown.Core/CloseApps/ProcessSnapshot.cs` — 进程身份快照（仅 PID/可执行路径/会话/启动时间，不含命令行、窗口标题、环境变量）。
- `src/AutoShutdown.Core/CloseApps/ForceKillResult.cs` — `ForceKillStatus` + `ForceKillResult`（强杀结局结构化）。

**新增（Core 配置 / 服务 / 动作）**
- `src/AutoShutdown.Core/Configuration/CloseAppsConfig.cs` — `CloseAppsConfig` + `CloseAppsTargetConfig`（向后兼容；强杀默认关闭）。
- `src/AutoShutdown.Core/CloseApps/CloseAppStatus.cs` — 单目标关闭结局枚举（9 态）。
- `src/AutoShutdown.Core/CloseApps/CloseAppTarget.cs` — 规范化关闭目标（脱敏 TargetId）。
- `src/AutoShutdown.Core/CloseApps/CloseAppTargetResult.cs` — 单目标结果（含 `ForceKillAuthorized` 审计）。
- `src/AutoShutdown.Core/CloseApps/CloseAppsReport.cs` — 总体报告（脱敏、限长摘要）。
- `src/AutoShutdown.Core/CloseApps/CloseAppsTargetList.cs` — 清单校验（路径/pid 恰取其一、超时边界、重复、null）+ 规范化。
- `src/AutoShutdown.Core/CloseApps/CloseAppsService.cs` — 编排（精确匹配→优雅关闭→有界等待→逐目标授权强杀；自身/会话/系统关键保护；fail-closed）。
- `src/AutoShutdown.Core/CloseApps/CloseAppsAction.cs` — `IPreShutdownAction`（Name=CloseApps，默认 `FailurePolicy.Block`）。

**新增（App 真机网关）**
- `src/AutoShutdown.App/Infrastructure/CloseApps/DiagnosticProcessManager.cs` — 真实进程枚举/身份（防御性容错，访问拒绝/已退出返回 null/哨兵）。
- `src/AutoShutdown.App/Infrastructure/CloseApps/DiagnosticAppWindowManager.cs` — 真实窗口/进程关闭（`CloseMainWindow` = WM_CLOSE；`Kill` 仅在强杀分支且经启动时间复核）。

**修改（接线与回归）**
- `src/AutoShutdown.Core/Configuration/AppConfig.cs` — 增加 `CloseApps` 配置段（默认空目标 = 不关闭任何应用）。
- `src/AutoShutdown.Core/Configuration/ConfigurationValidator.cs` — 增加 `ValidateCloseApps`（null/非法目标视为非法）。
- `src/AutoShutdown.App/AppHost/ServiceRegistration.cs` — 注册 `IProcessManager`/`IAppWindowManager`/`CloseAppsService`，`IPrePipelineRunner` 挂载 `CloseAppsAction`（置于 `OfficeSaveAction` 之后、流水线末尾）。
- `tests/AutoShutdown.Tests/S16_FakeActionSliceTests.cs` — 组合根冒烟断言更新为「OfficeSave→CloseApps 流水线 + GuardedPowerService + 托管 Process 网关」。

**新增（测试）**
- `tests/AutoShutdown.Tests/S18_CloseAppsSourceContractTests.cs` — 源码契约 4 项（无 P/Invoke/Process.Start/shell；优雅优先 + PID 复核；不触碰电源服务）。
- `tests/AutoShutdown.Tests/S18_CloseAppsConfigTests.cs` — 清单校验 15 项。
- `tests/AutoShutdown.Tests/S18_CloseAppsServiceTests.cs` — 编排策略 25 项。
- `tests/AutoShutdown.Tests/S18_CloseAppsActionTests.cs` — 动作契约 7 项。
- `tests/AutoShutdown.Tests/S18_RealMachineAcceptanceTests.cs` — 真机验收 1 项（门控）。

合计：23 文件，+2193 / -5。

## 测试与构建证据

| 项 | 结果 |
|----|------|
| S18 C1-C4 相关测试（Debug） | 52 通过 / 0 失败 / 0 跳过 |
| S18-D1 后 S18 相关测试（Debug，`~S18` 过滤） | 73 通过 / 0 失败 / 0 跳过 |
| S18-D2 后 S18 相关测试（Debug，`~S18` 过滤） | 79 通过 / 0 失败 / 0 跳过 |
| Debug 全量测试 | 850 通过 / 0 失败 / 0 跳过 |
| Release 全量测试 | 850 通过 / 0 失败 / 0 跳过（退出码 0） |
| Release 构建 | 0 警告 / 0 错误（TreatWarningsAsErrors 生效，退出码 0） |
| 源码契约测试（`~Contract` 过滤，Release） | 84 通过 / 0 失败 / 0 跳过 |
| 本阶段新增测试（C1-C4） | 52 项（4 + 15 + 25 + 7 + 1） |
| D1 返修新增测试 | 21 项（5 服务 + 3 e2e + 13 UI） |
| D2 返修新增测试 | 6 项（4 服务 + 2 e2e） |

注：Debug 全量一次运行中 `SchedulerEngineTests.CommandAndDeadlineCompletingTogether_CommandWinsWithoutFiring` 出现 2000ms 偶发超时（既有测试，S17 已记录同类偶发，与 S18 无关）；单独重跑 <1ms 通过，Release 全量 823 全过，判定为既有偶发。D1 返修的 Debug/Release 全量（844）运行未复现该偶发。

## 候选 EXE 检查点（G1-PKG-S18）

- 产物：`src/AutoShutdown.App/bin/Release/net8.0-windows/AutoShutdown.App.exe`
- 大小：524800 字节
- SHA256（D1 返修后）：`da7d537d1edf068a1c526f38fc6d98d8f4ca289597e8521184e405150fddb7dc`
- SHA256（D2 返修后）：`72e033769a63311c1fdf8c74cccd79813235a153a8ce90f45c8be7823ced25c5`
- 性质：含 CloseAppsAction 的候选检查点；生产默认 = `PrePipelineRunner([OfficeSave, CloseApps])` + `GuardedPowerService`；CloseApps 网关走托管 `System.Diagnostics.Process`（`CloseMainWindow` = WM_CLOSE + 强杀前 PID/启动时间复核），无 P/Invoke、无 `Process.Start`、无 shell。D1 后新增：可取消有界等待、三态退出状态、强杀后退出确认、设置页逐目标强杀授权 UI。

## 关键安全不变量核对

- **永不关闭 AutoShutdown 自身**：`IsProtected` 检查 `CurrentProcessId` → `SkippedProtected`（测试覆盖）。
- **永不关闭系统关键进程**：`CriticalProcessNames` 关键名黑名单 → `SkippedProtected`。
- **永不关闭非当前用户会话进程**：`SessionId` 与当前会话不符或未知（`-1`）→ `SkippedProtected`（D1-2：未知会话 fail-closed，绝不放行）。
- **永不关闭未被明确稳定规则匹配的进程**：仅精确「可执行路径」或「进程 ID」匹配；`ProcessSnapshot` 不含窗口标题/命令行，无模糊标题匹配。
- **默认只允许优雅关闭**：先 `RequestClose`（WM_CLOSE）→ 有界等待；强杀仅在 `ForceKillAllowed` 时。
- **强杀必须逐目标显式 opt-in**：`ForceKillAllowed` 默认 false；未授权绝不 `Kill`（测试断言 `ForceKillCalls` 为空）。
- **强杀前重新核对 PID 与启动时间**：服务重读 `GetProcessById` 比对启动时间、网关 `ForceKill` 复核 `freshStart == expectedStartTimeUtc`，不符返回 `PidReuseDetected`。
- **访问拒绝/进程已退出/窗口消失/等待竞态安全收敛**：`AccessDenied`/`AlreadyExited`/`NoWindow`/`TimedOut`/`PidReuseDetected` 结构化收敛。
- **状态或身份不明确时不得强杀**：启动时间未知（default）时强杀前复核拒绝。
- **不得新增第二个电源出口**：`CloseAppsAction`/`CloseAppsService` 不触碰电源服务边界（源码契约测试断言）；`ShutdownWorkflow` 仍唯一。
- **不得使用 cmd/PowerShell/shell/批处理**：源码契约断言网关无 `DllImport`/`Process.Start`/`shutdown.exe`/`cmd.exe`/`powershell`/`taskkill`/`ExitWindowsEx`/`SetSuspendState`。
- **损坏配置不得静默回退**：配置不可用/清单非法/CloseApps 为 null → `Failed`（fail-closed），不触发任何关闭/强杀。
- **测试用替身**：全程 Fake ProcessManager/AppWindowManager/ConfigurationService；真机用例门控默认 no-op，未触发真实进程或真实电源。

**D1 返修新增不变量**

- **取消后强杀 = 0**：等待期间取消或等待超时后、强杀前取消，一律以 OCE 传播，绝不 `ForceKill`、绝不电源（D1-1 测试断言 `ForceKillCalls` 为空）。
- **未知会话全关闭/等待/强杀 = 0**：`SessionId == -1` → `SkippedProtected`，RequestClose/WaitForExit/ForceKill 全 0（D1-2 测试断言）。
- **退出状态无法确认绝不报成功**：三态 `Unknown` 绝不映射 `AlreadyExited`，绝不强杀（D1-3 测试断言）。
- **强杀未确认绝不报成功**：`ExitNotConfirmed` 绝不映射 `ForceKilled`（D1-4 测试断言）。
- **上述安全失败路径 power=0**：D1 e2e 测试断言 `PrePipelineBlocked` + 空电源请求。

**D2 返修新增不变量**

- **等待异常后「无法确认」绝不当作超时**：三态 `Unknown` 绝不映射 `TimedOut`、绝不进入强杀路径（D2-1 测试断言 `ForceKillCalls` 为空）。
- **Unknown 路径绝不强杀、power=0**：等待异常复核 Unknown → `ExitStatusUnknown` → CloseAppsAction 默认 Block → 电源请求为空（D2-2 e2e 断言）。
- **确定超时保留强杀语义**：未授权强杀 → `TimedOut` 且 `ForceKillCalls` 为空；授权强杀 → 进入既有强杀路径（PID/启动时间复核 + 强杀后确认）（D2-3 测试断言）。
- **取消以 OCE 原样传播、强杀=0、power=0**：等待期间取消绝不折叠为 Unknown/TimedOut（D2-4 测试断言 `ThrowsAnyAsync<OperationCanceledException>` + 空 `ForceKillCalls`/空电源请求）。
- **布尔不丢失安全状态**：`WaitForExit` 不再返回 `bool`，无法通过布尔返回值把「无法确认」折叠成 `false`（源码层 `ProcessWaitResult` 三态）。

## 未执行的真机项目（人工待验）

- 真实 Notepad 专属实例优雅关闭验收：已编码为 `S18_RealMachineAcceptanceTests`（门控 `S18_REALMACHINE=1`），本次自动化运行未触发真实进程（no-op）。需人工执行：`S18_REALMACHINE=1 dotnet test --filter "Category=RealMachine"`。
- 候选 EXE 真实桌面 GUI 启动冒烟：未执行（仅组合根 DI 冒烟）；D1 新增的设置页「关闭应用（关机前）」卡片（逐目标强杀勾选 + 风险提示）未经真实桌面冒烟，需人工点验。
- 真实应用关闭 / 真实电源：均未执行（全程 Fake 替身 + 门控；未触碰真实进程或真实电源）。

## 遗留问题（≤3）

1. 真机 Notepad 验收未人工执行（门控用例，需在有桌面会话的机器上运行）。
2. 既有偶发超时测试 `SchedulerEngineTests.CommandAndDeadlineCompletingTogether_CommandWinsWithoutFiring`（非 S18 引入）建议后续排查。
3. `RunCommandsAction` 仍属后续阶段未实现（固定顺序中间项）；当前流水线为 `OfficeSave→CloseApps`，后续补入 RunCommands 到中间位即恢复完整固定顺序。

## 是否可进入下一阶段

**可进入下一阶段。** S18（CloseApps）完成，四项检查点（C1-C4）均已提交；S18-D1 返修（4 处安全缺陷 + 强杀授权 UI）与 S18-D2 返修（优雅等待结果三态）已完成并提交。Release 全量 850 通过 / 0 失败 / 0 跳过，Release 构建 0 警告 0 错误，安全源码契约全过，D1/D2 安全不变量（取消后强杀=0、未知会话全 0、未确认退出/未确认强杀绝不报成功、安全失败 power=0、等待异常 Unknown 绝不当作超时/强杀、取消以 OCE 原样传播）均有回归与 e2e 测试覆盖。CloseApps 以最后一个动作接入当前流水线（RunCommands 缺失不影响 `OfficeSave→RunCommands→CloseApps` 的最终固定顺序，后续补入 RunCommands 到中间位即可）。本阶段未提前进入 RunCommands 或后续阶段。
