# AutoShutdown S12.7 UI 修复工作日志

> 状态：已完成
> 日期：2026-08-13
> 项目根目录：`D:\电脑定时关机重建完整版`
> 背景：S12.6 基础版验收通过后，用户在人工使用中发现若干前台 UI 问题，本日志记录修复过程与结果。
> 说明：本阶段仅修改 UI 布局/显示逻辑，不涉及 Core、状态机、调度器、电源实现。

---

## 一、修复清单

### 1. 倒计时数字缩放消失（DropShadowEffect + Viewbox 渲染 bug）

- **现象**：主界面"当前任务"卡片的倒计时数字，在窗口反复放大/缩小时**完全消失**（不是变小，是看不到），放到一定比例又恢复。
- **根因**：倒计时 `TextBlock` 带 `DropShadowEffect`（发光阴影），放进 `Viewbox`（等比缩放容器）后，窗口快速缩放触发效果渲染层失效——WPF 已知渲染 bug。
- **修复**（`src\AutoShutdown.App\MainWindow.xaml`）：
  - 保留 `DropShadowEffect`（光晕视觉不变）
  - 给 `TextBlock` 增加 `CacheMode="BitmapCache"`，让 Viewbox 缩放时缓存位图、不再反复触发效果重绘
- **验证**：反复点放大/缩小按钮，倒计时数字不再消失。
- **备选方案**（未采用，留档）：若方案 A 仍失效，可将光晕独立为背景 `Ellipse`+`BlurEffect`，文本本身不带 Effect。

### 2. "下次执行时间 / 提前提醒 / 任务摘要"显示 UTC 时间（差 8 小时）

- **现象**：用户感觉"下次执行时间在乱报时间"。
- **根因**：`ScheduledFireTime` / `WarningStartTime` 内部统一存 **UTC**，但界面显示时直接用 `ToString()` 未转本地时区，导致显示比本地时间（UTC+8）早 8 小时。
- **修复**（`src\AutoShutdown.App\Presentation\MainWindowViewModel.cs`，`Refresh()` 内）：
  - `NextFireTimeText`：`TimeZoneInfo.ConvertTime(ScheduledFireTime, _clock.LocalTimeZone)` 后格式化
  - `WarningTimeText`：`TimeZoneInfo.ConvertTime(WarningStartTime, _clock.LocalTimeZone)` 后格式化
  - `TaskSummaryText`：使用本地时间格式化
- **验证**：创建任务后界面显示本地时间，与预期一致。全量测试 408/408 通过。
- **说明**：倒计时（`remaining`）与提醒窗口（`ReminderWindow`）只显示时间差，不受时区影响，无需修改。

---

## 二、验证结果

- Release 构建：0 错误、0 警告 ✅
- 全量测试：408/408 通过，失败 0、跳过 0 ✅
- 人工验证：倒计时不再消失、时间显示为本地时间 ✅

---

## 三、修改文件清单

| 文件 | 修改内容 |
|---|---|
| `src\AutoShutdown.App\MainWindow.xaml` | 倒计时 `TextBlock` 加 `CacheMode="BitmapCache"` |
| `src\AutoShutdown.App\Presentation\MainWindowViewModel.cs` | 三处时间显示转本地时区 |

---

## 四、遗留说明

1. 本阶段修改已重新发布打包版（见 §六），最新包包含上述全部修复。
2. 备选方案 B（光晕独立化）未实施，留档备用。
3. 未涉及 Core、状态机、调度器、电源实现、注册表、自启动。

---

## 六、设置页按钮修复与重新发布（2026-08-14）

### 6.1 设置页按钮"字体挤 / 边框不明显"修复

**根因**：按钮自定义模板里的 `ContentPresenter` 未绑定 `Padding`，导致 `Padding` 属性不生效，文字与边框间距无法通过改数值调整。

**修复**（`src\AutoShutdown.App\Themes\Controls.xaml`）：
- `PrimaryButtonStyle`、`SecondaryButtonStyle` 模板的 `ContentPresenter` 增加 `Margin="{TemplateBinding Padding}"`，使内边距真正生效。
- 边框 `BorderThickness` 由 `1.5` 加粗至 `2`，提升浅色背景上的可辨识度。
- `SecondaryButtonStyle` 内边距 `18,9 → 22,10`。

**设置页按钮内边距**（`src\AutoShutdown.App\MainWindow.xaml`）：
- "启用开机自启动"：`Padding="26,10"`
- "关闭开机自启动"：`Padding="24,10"`

### 6.2 提醒窗口样式（Red 框区域，已于此前批次完成）

- 外层卡片边框 `#E8FFFFFF → #F2FFFFFF`（保持 2px）
- 标题"关机前提醒"：`White → TextPrimaryBrush`
- 副文本：`#E5ECFF → TextSecondaryBrush`
- 按钮改用 `ReminderButtonStyle`（实心蓝白字）

### 6.3 重新发布验证

- Release 构建：0 错误、0 警告 ✅
- 全量测试：408/408 通过，失败 0、跳过 0 ✅
- 发布包 SHA-256 与清单一致 ✅

**最新发布包：**
- 轻量版：`artifacts\release\v1.0.0\packages\AutoShutdown-v1.0.0-win-x64-framework-dependent.zip`
  - SHA-256：`53CC68E9DACFC7AB8F48778AC346066D244D7B7DD793A8A6459AB070AF856682`
- 免运行库版：`artifacts\release\v1.0.0\packages\AutoShutdown-v1.0.0-win-x64-self-contained.zip`
  - SHA-256：`706A6F1C590E5733A8DB737E77C273DAE3D28B10EBFEB4924DE234389BF94252`

---

## 五、后续

基础版 UI 修复完成后，可进入 v2 工作单规划（关机前关闭软件、Office 文档保存等高级功能）。
