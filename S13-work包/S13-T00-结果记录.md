# S13-T00 结果记录（强制真实基线 + 源码标识基线 + Gate 登记）

> 任务卡：`S13-work包/S13-实施任务卡集.md` → S13-T00
> 执行日期：2026-08-15（本机 Windows 11，非沙箱）
> 范围：仅 T00；未执行 T01；未修改任何业务代码、冻结架构、任务卡集或 UI 需求包。

---

## 1. 环境核验（通过）

| 项 | 实测值 | 结论 |
|----|--------|------|
| .NET SDK | `8.0.130`（`dotnet --version`） | ✅ 满足 .NET 8 要求 |
| 解决方案 | `AutoShutdown.sln`（VS2022 格式），含 3 项目 | ✅ |
| 项目 1 | `src/AutoShutdown.Core` — `net8.0` | ✅ |
| 项目 2 | `src/AutoShutdown.App` — `net8.0-windows` + WPF/WinForms | ✅ |
| 项目 3 | `tests/AutoShutdown.Tests` — `net8.0-windows` + xUnit 2.8.1 | ✅ |
| 构建配置 | `Directory.Build.props`：LangVersion=latest / Nullable=enable / ImplicitUsings=enable / **TreatWarningsAsErrors=true** | ✅ 与准备包一致 |
| NuGet | `NuGet.Config`：仅 nuget.org | ✅ |
| 测试文件数 | 22（`tests/AutoShutdown.Tests/*Tests.cs`） | ✅ 与准备包一致 |
| 版本控制 | 非 Git 仓库（本记录首次建立） | ✅ 与准备包一致 |

**环境阻断 E1（准备包 §10.4）解除**：本机具备 .NET 8 SDK，可实际执行 restore/build/test。

---

## 2. 准备包关键路径 vs 真实仓库核对（一致，无重大偏差）

| 准备包声明 | 真实仓库 | 结论 |
|-----------|---------|------|
| 启动链 `App.OnStartup → SingleInstanceCoordinator → ServiceRegistration → ApplicationLifetimeCoordinator` | `src/AutoShutdown.App/App.xaml.cs`、`Infrastructure/SingleInstanceCoordinator.cs`、`AppHost/ServiceRegistration.cs`、`AppHost/ApplicationLifetimeCoordinator.cs` 均存在 | ✅ 对应 |
| 状态族（`TaskState` 9 值、`TaskStateMachine`、`RuntimeState` 单实例） | `Core/State/Enums.cs`（Unknown=0,Idle,Scheduled,Warning,Executing,Cancelled,Completed,Failed,Interrupted）、`TaskStateMachine.cs`、`RuntimeState.cs` | ✅ 对应（`runtime.json` 实测为 `CurrentInstance` 单实例） |
| 调度族（`SchedulerEngine` 单实例、命令、结果） | `Core/Scheduling/SchedulerEngine.cs`、`SchedulerCommands.cs`、`SchedulerResults.cs` | ✅ 对应 |
| 工作流（`ShutdownWorkflow` 唯一电源出口 + `ShutdownScheduledTaskHandler`） | `Core/Workflow/` 两文件 | ✅ 对应 |
| 存储（`FileStorage` 原子写 + 备份） | `Core/Storage/FileStorage.cs` | ✅ 对应 |
| 配置（`ConfigurationService` + `AppConfig`，SchemaVersion=1） | `Core/Configuration/`（`config.json` 实测 SchemaVersion=1） | ✅ 对应 |
| 接口（12 个） | `Core/Abstractions/` 12 个接口文件 | ✅ 对应 |
| UI（`MainWindowViewModel` / `UiTextMapper` / `DashboardRefreshService`，导航任务管理占位） | `App/Presentation/` 三文件存在 | ✅ 对应 |
| 日志装饰器先例 | `App/Infrastructure/Logging/LoggingShutdownWorkflowDecorator.cs` | ✅ 对应（T04 引用） |
| 打包入口 | `tools/Publish-SafeRelease.ps1`（脚本内硬编码 SDK 路径 + `Get-Command dotnet` 回退） | ✅ 对应 |
| 发布产物 | `artifacts/release/v1.0.0/`（backups/manifests/packages/staging） | ✅ 存在 |
| 运行期数据 | `%LocalAppData%\AutoShutdown\`（`config.json` + `runtime.json` + `logs/` + `backups/`） | ✅ 存在，V1 单实例 schema |
| 测试覆盖 | 22 个测试文件（L0–L2 + S12 系列） | ✅ 对应 |
| 图标 | `assets/icon.ico` 及多尺寸 PNG/SVG | ✅ 存在 |
| V2 新符号不存在 | `TaskCollection` / `TaskArbitrator` / `CrashRecoveryManager` / `TaskInstanceState` / `tasks.json` 全仓库 0 命中 | ✅ 确认不存在 |

**结论**：准备包 §3/§5/§6 关键路径与真实仓库一一对应，**未发现模块改名/消失的重大不一致**。T00 停止条件之一（准备包与实际仓库重大不一致）未触发。

---

## 3. 强制真实基线（原始证据）

### 3.1 restore

```
$ dotnet restore AutoShutdown.sln
  所有项目均是最新的，无法还原。
RESTORE_EXIT:0
```

### 3.2 build（Release）

```
$ dotnet build AutoShutdown.sln -c Release
  适用于 .NET MSBuild 版本 17.8.49+7806cbf7b
  AutoShutdown.Core -> D:\电脑定时关机重建完整版\src\AutoShutdown.Core\bin\Release\net8.0\AutoShutdown.Core.dll
  AutoShutdown.App  -> D:\电脑定时关机重建完整版\src\AutoShutdown.App\bin\Release\net8.0-windows\AutoShutdown.App.dll
  AutoShutdown.Tests -> D:\电脑定时关机重建完整版\tests\AutoShutdown.Tests\bin\Release\net8.0-windows\AutoShutdown.Tests.dll
  已成功生成。
    0 个警告
    0 个错误
BUILD_EXIT:0
```

### 3.3 test（Release，solution 级全量）

```
$ dotnet test AutoShutdown.sln -c Release
  Microsoft (R) 测试执行命令行工具版本 17.8.0 (x64)
  已通过! - 失败: 0，通过: 408，已跳过: 0，总计: 408，持续时间: 2 s - AutoShutdown.Tests.dll (net8.0)
TEST_EXIT:0
```

### 3.4 测试基准（供 T09 回归对照）

| 指标 | 值 |
|------|----|
| 测试总数 | **408** |
| 通过 | **408** |
| 失败 | **0** |
| 跳过 | **0** |
| 既有失败基线 | **无（0 既有失败）** |
| 退出码 | 0 |

---

## 4. 源码标识基线（GATE-SCM，方案A）

- 决策：**方案 A（Git 仓库 + S13 基线提交）**——经人工确认。
- `.gitignore` 新建于仓库根：排除 `**/bin/`、`**/obj/`、`TestResults/`、`artifacts/`、`*.log`、`*.out`、`*.trx`、`.vs/`、`.vscode/`、`.dsh-vision-toolkit/`、`.workbuddy/`。
- 首次提交内容：166 个文件（src 91 / tests 23 / assets 10 / S13-work包 6 / tools 8 / docs 1 / 根级工单与配置文件），**不含任何构建产物**。
- **基线提交哈希：`fe4e46083a3dfcfbcd99eac8e238b988d4ae1220`**（短 `fe4e460`）
  - 提交信息：`S13-T00 baseline: AutoShutdown V2 pre-S13 source snapshot`
- 后续：T01–T09 每卡独立提交；回滚/diff/候选 EXE 追溯均以该提交为锚。

---

## 5. Gate 登记（T00 视角）

| Gate | 内容 | 状态 | 说明 |
|------|------|------|------|
| **GATE-SCM** | 源码版本绑定 | ✅ **已解决** | 方案A + 基线提交 `fe4e460` |
| GATE-G0S13 | G0-S13 正式批准记录位置 | ⚠️ 推定已过，待绑定 | 需人工绑定批准记录 |
| GATE-A2 | 转换白名单 15 vs 16 条 | ⚠️ 待裁决 | T01 以架构书 S13 §5 为冻结来源（卡内回退） |
| GATE-Q3 | 旧 runtime.json 策略 | ⚠️ 待裁决 | **阻断 T03 迁移验收项**，不阻断 T01 |
| GATE-Q4 | `TaskDefinition.Priority` 契约 | ⚠️ 待裁决 | **阻断 T02A 字段 / T06 排序项**，不阻断 T01 |
| GATE-A1 | PowerAction 强度表（含"注销"） | ⚠️ 待裁决 | **阻断 T06 强度表项**，不阻断 T01 |
| GATE-UI | 《V2 UI 实施基线》文档 | ⚠️ 缺失 | **只阻断 T08**，不阻断 T01–T07 |
| GATE-Q1/Q2 | EXE 构建号/保存目录 | ⚠️ 待裁决 | T09 前解决即可，不阻断前面 |

**GATE-SCM 决策记录**：2026-08-15 人工选定方案 A（Git init + S13 基线提交），本记录第 4 节为执行结果。

---

## 6. V2 UI 实施基线状态（仅记录，未修改）

- 检查点：`docs/ui-reference/` 下《V2 UI 实施基线》文档。
- 实测：`docs/ui-reference/` 仅存在 `S11-main-visual.jpg`（参考图，2.8MB），**不存在** UI 基线文档。
- 影响：GATE-UI 仍缺失 → **只阻断 T08**（UI Gate 未过，T08 不得开工），不阻断 T01–T07。
- 本次未补充、未扩写任何 UI 需求内容（按禁止边界执行）。

---

## 7. T00 回填的待定路径（定名定路径，各卡开工时复核）

| 用途 | 引用卡 | 建议路径 | 依据 |
|------|--------|---------|------|
| V2 8 态枚举 | T01 | 新增 `src/AutoShutdown.Core/State/TaskInstanceState.cs`（独立枚举，保留 V1 `TaskState`；T07 完成后可考虑清理 V1） | 架构书 §4.4；`PowerAction` 不得删除；独立枚举减少破坏面 |
| tasks.json 文档封装 | T02B | 新增 `src/AutoShutdown.Core/Storage/TasksDocumentStorage.cs`（复用 `FileStorage` 语义，不改造其本体） | Storage 目录既有命名 |
| 仲裁接口 | T04 | 新增 `src/AutoShutdown.Core/Abstractions/ITaskArbitrator.cs`（接口归 Abstractions 惯例） | 现有 12 接口均在此目录 |
| 仲裁实现 | T06 | `src/AutoShutdown.Core/Scheduling/TaskArbitrator.cs`（任务卡已指定） | — |
| CrashRecoveryManager 接入点 | T05 | 新增 `src/AutoShutdown.Core/Recovery/CrashRecoveryManager.cs`；接入 `src/AutoShutdown.App/AppHost/ApplicationLifetimeCoordinator.cs`（调度循环前） | 任务卡已指定；接入点实测存在 |
| UI 列表 ViewModel | T08 | 新增 `src/AutoShutdown.App/Presentation/TaskListViewModel.cs` | Presentation 目录惯例 |
| UI 列表视图 | T08 | 新增 `src/AutoShutdown.App/Presentation/TaskListView.xaml`（仓库当前无 `Views/` 目录，T08 确认是否新建目录） | 与 ViewModel 同目录，待 T08 复核 |

> 以上为 T00 产出（定名定路径），**非实施动作**；各卡开工时按各自"允许读取/修改"边界复核。

---

## 8. T01 阻断评估与下一批建议

### 阻断 T01 的问题

- **无硬性技术阻断**：基线 restore/build/test 全绿（0 错误 / 0 失败）；GATE-SCM 已解决；代码地图无重大不一致。
- **建议实施前人工裁决（不硬阻断 T01）**：
  1. **GATE-G0S13** —— 绑定 G0-S13 状态机设计评审 + I1~I9 未破坏的正式批准记录；
  2. **GATE-A2** —— 裁决转换白名单 15 条 vs 16 条；若未裁决，T01 按卡内回退以架构书 S13 §5 表为冻结来源。
- 其余 Gate（Q3/Q4/A1/UI）分别阻断 T03/T02A/T06/T08，**不阻断 T01**。

### 下一批建议（建议顺序）

1. **先裁决 GATE-G0S13、GATE-A2**（实施前六项中的前两项，本卡基线已就绪）；
2. 依次执行 **T01 → T02A/T02B → T03 → T04**；
3. T04 完成后 T05 独占串行迁出；T06/T07 文件边界内并行；T08 前补 GATE-UI；T09 前解 GATE-Q1/Q2；
4. T09 回归基准 = 本记录 §3.4（408 通过 / 0 失败 / 0 既有失败）。

---

## 9. 禁止边界执行声明

- 未修改 `src/**`、`tests/**`、`tools/**`、`*.sln`、`*.props`、`NuGet.Config`；
- 未修改现有 `S13-work包/*.md`（仅新增本记录）；
- 未执行 T01，未修改冻结架构，未重新生成任务卡集，未扩写 UI 需求包；
- 全部检查均为真实执行，无"无法运行的检查写成已通过"。
