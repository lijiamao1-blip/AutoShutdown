# S13-T07 结果记录（Workflow 双闸门适配）

> 任务卡：`S13-work包/S13-实施任务卡集.md` → S13-T07（Workflow 适配）
> 执行日期：2026-08-16
> 范围：仅 T07；未执行 T08（UI 切片）、T09（合流与候选 EXE）。
> 前置：T03/T04 合并切换（`842cb1b`）、T05（`ef28a8d`）、T06（`adad66a`）已落地。

---

## 1. 交付结论

| 目标 | 状态 |
|------|------|
| ShutdownWorkflow 使用 V2 实例状态 | ✅ `instance.State != TaskInstanceState.Executing`（V2），T03/T04 已切换 |
| task id 定位 | ✅ 校验 `InstanceId / SourceTaskId / StageToken` 均非空 |
| 保持 `config.RealPowerEnabled` + `instance.RealPowerConfirmed` 双闸门 | ✅ 非 TestMode 下逐闸门校验 |
| 不改变 PowerRequest/PowerResult/GuardedPowerService/Win32PowerService | ✅ 零改动 |
| 不增加任何电源调用点 | ✅ 唯一电源出口仍为 `ShutdownWorkflow`（结构性测试锁定） |
| 全量测试 | ✅ 551 通过 / 0 失败 |
| Release build | ✅ 0 警告 / 0 错误 |

---

## 2. 现状说明（为何本卡以验证 + 锁定为主）

- T03/T04 合并切换时已对 `ShutdownWorkflow` 做「最小适配」：仅 `TaskState.Executing → TaskInstanceState.Executing` 一行，双闸门与电源语义未变。
- 双闸门（`config.TestMode` 分支之外的 `RealPowerEnabled` + `RealPowerConfirmed`）来自 S12.5 基线，V2 切面下保持原样。
- 因此本卡不新增生产逻辑改动，而是：**① 结构性锁定唯一电源出口；② 补齐 V2 状态闸门的全枚举覆盖（原仅测 `Waiting`）；③ 双闸门端到端复验**，形成可复核的 T07 独立检查点。

---

## 3. 交付物

### 3.1 `tests/AutoShutdown.Tests/S13_T07_WorkflowGateTests.cs`（新增，10 项）

| 用例 | 断言 |
|------|------|
| `OnlyShutdownWorkflowConsumesIPowerServiceInCore` | 反射遍历 Core 程序集：唯一以 `IPowerService` 作为构造参数或字段的类是 `ShutdownWorkflow`（唯一电源出口） |
| `Execute_WhenStateIsNotExecuting_RejectsInvalidState_NoPower`（Theory × 8：Unknown/Waiting/Running/Confirming/Executed/Cancelled/Faulted/Interrupted） | 所有非 Executing 的 V2 态一律 `InvalidState` + 零电源调用 |
| `RealPowerMode_RequiresEnabledAndConfirmed` | 闸门一：`RealPowerEnabled=false` → `RealPowerNotEnabled` 零调用；闸门二：`RealPowerEnabled=true` 且未确认 → `RealPowerConfirmationMissing` 零调用 |

### 3.2 既有覆盖（复验通过，未改动）

- `S12_5WorkflowGateTests`（4 项）：双闸门三态 + TestMode 模拟路径。
- `ShutdownWorkflowTests`（20+ 项）：配置不可用/异常、状态闸门、身份、动作、时序、电源结果三态、取消传播、请求独立性。
- `S12_5PowerGateTests`（6 项）：`GuardedPowerService` 四分支路由，零真实电源。

---

## 4. 接入文件

| 文件 | 改动 |
|------|------|
| `tests/AutoShutdown.Tests/S13_T07_WorkflowGateTests.cs` | 新增：唯一出口 + V2 状态闸门全枚举 + 双闸门复验 |

**未触碰（禁改 / 本卡范围外）**：`PowerRequest.cs`、`PowerResult.cs`、`GuardedPowerService.cs`、`Win32PowerService.cs`、`FakePowerService.cs`、`ShutdownWorkflow.cs`（T03/T04 后已正确）、`Enums.cs`、`FileStorage.cs`。

---

## 5. 测试与构建结果

### 5.1 T07 定向测试

```
$ dotnet test AutoShutdown.sln -c Release --filter "FullyQualifiedName~S13_T07_WorkflowGateTests"
  已通过! - 失败: 0，通过: 10，已跳过: 0，总计: 10
```

### 5.2 Release 全量测试

```
$ dotnet test AutoShutdown.sln -c Release
  已通过! - 失败: 0，通过: 551，已跳过: 0，总计: 551
```

（T06 为 541；T07 净 +10。）

### 5.3 Release build

```
$ dotnet build AutoShutdown.sln -c Release
  已成功生成。0 个警告，0 个错误
```

---

## 6. 提交

- 实现提交：`f29e60d`（`S13-T07: verify Workflow V2 double-gate + unique power exit`）
- 结果记录提交：本记录（紧随其后）。
- 工作树干净（仅余参考文档 `S13-work包/AutoShutdown-V2-S13-阶段一键执行书.docx`，非交付物，不纳入提交）。

---

## 7. 遗留 / 开放项（记入结果，不阻断）

1. **仲裁结果消费方落实归 T09**：`SchedulerEngine.TryProcessDueAsync` 对多到期仍 `SetFaulted`（占位注释），正式「赢家执行 + 落选改期」由 T09 闭合（T06 仅实现决策器）。
2. **「强制」任务标志数据模型缺口**：沿用 T06 记录，`forcedTaskIds` 为独立注入占位。
3. **`Running`（Pre-Pipeline）瞬态**、**V1 状态机文件为死代码**：沿用 T03/T04 记录，本卡未改动。

---

## 8. 禁止边界执行声明

- 未修改 `PowerRequest.cs` / `PowerResult.cs` / `GuardedPowerService.cs` / `Win32PowerService.cs` / `FakePowerService.cs` / `ShutdownWorkflow.cs` / `Enums.cs` / `FileStorage.cs`。
- 唯一电源出口仍为 `ShutdownWorkflow`（已由结构测试锁定）。
- 自动化测试全部使用替身电源（RecordingPowerService），未触发真实关机/重启/休眠。
- 未触碰 T08 / T09 范围。全部检查真实执行，无「无法运行的检查写成已通过」。
