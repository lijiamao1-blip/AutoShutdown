# S13 实施准备包（独立版 · 修订）

> 工作包：S13 · 项目接入与实施准备
> 需求依据：R-001（F1 多任务）；承接 R-012 后续流水线任务基础
> 架构基线：V2-DRAFT-004（修正版）
> 扫描日期：2026-08-15
> 性质：只读扫描；未修改任何业务代码、配置、Schema 或依赖
> 修订说明：按审查意见修正命令路径、术语（实施差距/环境阻断/已批准决策）、Gate 证据表述与语言版本措辞

---

## 0. 能力闸门

| 能力 | 结论 |
|------|------|
| 文件读取 | 可用 |
| 全文搜索 | 可用 |
| 命令执行 | 部分可用（Linux 沙箱，无 .NET SDK） |
| 构建验证 | **环境能力不满足**（`dotnet` 不存在，仅能识别配置） |
| 测试验证 | **环境能力不满足**（同上，仅能识别配置） |
| 打包验证 | **环境能力不满足**（PowerShell 发布脚本需 Windows 主机） |
| 版本控制 | 不适用（目录非 git 仓库，无分支/提交可报告） |

> 除"环境能力不满足"外，本文档所有技术事实均来自只读扫描，可复核，无伪造。

---

## 1. 仓库 / 源码标识

- 源码根目录：`D:\电脑定时关机重建完整版\`
- 解决方案：`AutoShutdown.sln`（VS 2022，含 3 个项目）
  1. `src/AutoShutdown.Core/AutoShutdown.Core.csproj` — `net8.0`，纯逻辑层
  2. `src/AutoShutdown.App/AutoShutdown.App.csproj` — `net8.0-windows`，WPF 宿主
  3. `tests/AutoShutdown.Tests/AutoShutdown.Tests.csproj` — `net8.0-windows`，xUnit 测试
- 版本控制：无（`git` 报告 not a git repository）
- 构建配置：`Directory.Build.props`（LangVersion=latest / Nullable=enable / ImplicitUsings=enable / TreatWarningsAsErrors=true）；`NuGet.Config`（仅 nuget.org）
- 已存在的发布产物：`artifacts/release/v1.0.0/`（含 `backups/`、`manifests/`、`packages/AutoShutdown-v1.0.0-win-x64-*.zip`）
- 运行期数据目录：`%LocalAppData%\AutoShutdown\`（`config.json`、`runtime.json`、`logs/`，由代码在运行时生成，仓库内无这些 json）

---

## 2. 技术栈事实

| 项 | 值 | 证据 |
|----|----|------|
| 语言 | `LangVersion=latest`（未写死为 `12.0`；在目标 .NET 8 SDK 下预期对应 C# 12，最终以实际 SDK 编译结果为准） | `Directory.Build.props` |
| 运行时 | .NET 8.0 | 各 csproj `TargetFramework` |
| 平台 | 仅 Windows | App/Tests 为 `net8.0-windows` |
| UI | WPF（`UseWPF=true`）+ WinForms 可用 | `AutoShutdown.App.csproj` |
| DI | Microsoft.Extensions.DependencyInjection 8.0.1 | 同上 |
| 测试 | xUnit 2.8.1、Microsoft.NET.Test.Sdk 17.10.0、xunit.runner.visualstudio 2.8.1 | `AutoShutdown.Tests.csproj` |
| JSON | System.Text.Json（`FileStorage` 使用） | `FileStorage.cs` |
| 打包 | `tools/Publish-SafeRelease.ps1`（win-x64；轻量版 + 免运行库版） | `tools/` |
| 图标 | `assets/icon.ico`（EXE 图标 + WPF 资源）及多尺寸 PNG/SVG | `assets/` |

---

## 3. 代码地图

### 3.1 启动链（真实入口）

```
App.OnStartup()  [src/AutoShutdown.App/App.xaml.cs]
  └─ 单实例检查 SingleInstanceCoordinator.TryAcquirePrimary()   （Mutex，I9）
  └─ ServiceCollection + AddAutoShutdownServices()               （DI 组装）
  └─ ApplicationLifetimeCoordinator.Start()                      [AppHost/]
       ├─ ActivationPipeServer.Start()                           （Pipe，I9）
       ├─ SchedulerEngine.RunAsync(ct)                           （调度循环）
       ├─ TrayIconService.Start()
       ├─ DashboardRefreshService.Start()                        （800ms 轮询 GetSnapshot → ViewModel.Refresh）
       ├─ NotificationCoordinator.Start()
       └─ WindowActivationService.ActivateMainWindow()
```

关键文件：
- `src/AutoShutdown.App/App.xaml.cs`
- `src/AutoShutdown.App/AppHost/ServiceRegistration.cs`
- `src/AutoShutdown.App/AppHost/ApplicationLifetimeCoordinator.cs`
- `src/AutoShutdown.App/MainWindow.xaml(.cs)`、`Presentation/MainWindowViewModel.cs`、`Presentation/DashboardRefreshService.cs`、`Presentation/UiTextMapper.cs`

### 3.2 S13 相关符号现状

**已存在（将被 S13 改造）**：

| 符号 | 位置 | 现状要点 |
|------|------|----------|
| `TaskState` 枚举 | `Core/State/Enums.cs` | 9 值：Unknown=0, Idle, Scheduled, Warning, Executing, Cancelled, Completed, Failed, Interrupted |
| `TaskKind` / `PowerAction` | `Core/State/Enums.cs` | TaskKind: Countdown/TodayAt/DailyAt；PowerAction: Shutdown/Restart/Sleep/Hibernate |
| `TaskStateMachine` | `Core/State/TaskStateMachine.cs` | `FrozenDictionary<(Current,Target),Cause>` 白名单；校验 Unknown 态/未知 cause；终态仅可经 `ClearTerminalState` 回 Idle |
| `TaskTransitionCause` | `Core/State/TaskTransitionCause.cs` | 10 值（含 Schedule/WarningDue/ExecuteDue/CancelByUser/SnoozeByUser/Reschedule/PowerAccepted/PowerFailed/RecoveryInterrupted/ClearTerminalState） |
| `TaskInstance` | `Core/State/RuntimeState.cs` | 单任务 record：InstanceId/SourceTaskId/ActionSnapshot/State/ScheduledFireTime/WarningStartTime/StageToken/HasExecuted/CreatedAt/RealPowerConfirmed/RealPowerConfirmationId |
| `RuntimeState` | `Core/State/RuntimeState.cs` | `CurrentInstance`（单一实例）+ SchemaVersion + LastUpdatedAt |
| `TaskService` / `ITaskService` | `Core/Tasks/` | Create/Snooze/Cancel/RescheduleDaily，单任务 |
| `TaskDefinition` | `Core/Tasks/TaskDefinition.cs` | Id/Kind/Action/CountdownDuration/TargetTimeOfDay/WarningSeconds/CreatedAt/RealPowerConfirmed |
| `SchedulerEngine` / `ISchedulerEngine` | `Core/Scheduling/` | 单实例：Channel 命令队列 + 单 deadline + `CurrentInstance`；恢复逻辑内联在 `TryRestoreAsync`（Warning/Executing → Interrupted，仅写一次）；missed scheduled 直接 Faulted |
| `SchedulerCommand` 族 | `Core/Scheduling/SchedulerCommands.cs` | CreateTaskCommand / SnoozeTaskCommand / CancelTaskCommand / ClearTerminalTaskCommand |
| `ShutdownWorkflow` / `IShutdownWorkflow` | `Core/Workflow/` | 唯一电源出口（I2）：加载配置→双闸门（config.RealPowerEnabled + instance.RealPowerConfirmed）→校验 State==Executing 且 HasExecuted→调 `IPowerService` |
| `ShutdownScheduledTaskHandler` | `Core/Workflow/` | 包装 workflow；失败抛 `ScheduledTaskHandlingException` |
| `NextExecutionCalculator` | `Core/Scheduling/` | Countdown/TodayAt/DailyAt 计算 |
| `FileStorage` / `IStorage` | `Core/Storage/` | 原子写（临时文件→File.Replace→备份）；path 越界校验；写闸门；写前反序列化自检 |
| `ConfigurationService` / `AppConfig` | `Core/Configuration/` | 仅 `config.json`；schema 迁移链接口存在但当前无迁移项 |
| UI | `App/Presentation/MainWindowViewModel.cs` | 单任务状态卡 + 创建/延迟/取消/清除命令；导航"任务管理"为占位页（`IsPlaceholder=true`，"后续开放"） |

**不存在（S13 需新建，全文搜索 0 命中）**：
- `TaskCollection`
- `TaskArbitrator`
- `CrashRecoveryManager`
- `TaskInstanceState`（V2 8 态枚举）
- `tasks.json` 相关类型/引用

### 3.3 调用关系（只读确认）

```
ViewModel ─GetSnapshot/SubmitAsync→ ISchedulerEngine
ViewModel ─LoadAsync/SaveAsync────→ IConfigurationService
DashboardRefreshService ─GetSnapshot→ ISchedulerEngine（800ms）

SchedulerEngine ─→ ITaskStateMachine / ITaskService / IScheduledTaskHandler
               ─→ IStorage(runtime.json) / IClock / IAsyncDeadline
ShutdownScheduledTaskHandler ─→ IShutdownWorkflow ─→ ShutdownWorkflow ─→ IPowerService  （I2）
FileStorage ─→ config.json / runtime.json
```

### 3.4 持久化现状

| 文件 | 状态 | 说明 |
|------|------|------|
| `config.json` | 由运行时生成（SchemaVersion=1） | 全局配置，`ConfigurationService` 读写 |
| `runtime.json` | 由运行时生成（SchemaVersion=1） | 单实例 `CurrentInstance` |
| `tasks.json` | 不存在 | 架构 V2 新增，S13 需定义 |

### 3.5 现有测试（L0–L2 覆盖）

| 测试文件 | 聚焦 |
|----------|------|
| `TaskStateMachineTests` | 转换白名单 / cause 不匹配 / 非法对拒绝 / 终态清空 |
| `SchedulerEngineTests` | 启动/恢复/创建/延迟/取消/清除/持久化失败/恢复不补执行/工作流集成 |
| `CorePipelineIntegrationTests` | 端到端执行恰好一次、取消零执行、崩溃恢复零执行、持久化失败安全 |
| `TaskServiceTests` | 任务 CRUD 校验 |
| `NextExecutionCalculatorTests` | 三种调度计算 |
| `ShutdownWorkflowTests` | 双闸门 / 状态 / 电源结果分派 |
| `FileStorageTests` | 原子写 / 备份 / 损坏检测 |
| `ConfigurationServiceTests` | 配置加载校验 / 迁移 |
| `S11UiSourceContractTests` / `AppHostSourceContractTests` | UI 源契约 / DI 组合与无真实电源 API 泄漏 |
| 其余 S12 系列 | 日志 / 自启 / 首启 / 图标 / 发布 / 电源闸门 |

---

## 4. 契约索引

### 4.1 现有接口（12 个，`Core/Abstractions/`）

| 接口 | 签名要点 |
|------|----------|
| `IClock` | `UtcNow`、`LocalTimeZone` |
| `IAsyncDeadline` | `Task WaitUntilAsync(DateTimeOffset, CancellationToken)` |
| `IIdentifierGenerator` | `Guid NewId()` |
| `IStorage` | `ReadAsync<T>`、`WriteAsync<T>` |
| `IConfigurationService` | `LoadAsync` / `SaveAsync(AppConfig)` / `CreateSafeDefaultAsync` |
| `INextExecutionCalculator` | `NextExecutionResult Calculate(TaskDefinition, DateTimeOffset, TimeZoneInfo)` |
| `ITaskStateMachine` | `TaskTransitionResult TryTransition(TaskState, TaskState, TaskTransitionCause)` |
| `ITaskService` | `Create` / `Snooze` / `Cancel` / `RescheduleDaily` |
| `ISchedulerEngine` | `RunAsync` / `SubmitAsync` / `GetSnapshot` |
| `IScheduledTaskHandler` | `Task HandleDueAsync(TaskInstance, CancellationToken)` |
| `IShutdownWorkflow` | `Task<ShutdownWorkflowResult> ExecuteAsync(TaskInstance, CancellationToken)` |
| `IPowerService` | `Task<PowerResult> ExecuteAsync(PowerRequest, CancellationToken)` |

### 4.2 结果/命令类型

- 调度：`SchedulerSnapshot`（含 `CurrentInstance`）、`SchedulerCommandResult`、`SchedulerCommandStatus`、`SchedulerEngineStatus`
- 任务：`TaskCommandResult`、`TaskCommandStatus`
- 状态机：`TaskTransitionResult`、`TaskTransitionDecisionCode`
- 工作流：`ShutdownWorkflowResult`、`ShutdownWorkflowStatus`、`ShutdownDecisionCode`
- 电源：`PowerRequest`（含 `RealPowerConfirmed`）、`PowerResult`（含 `Outcome/WasSimulated/NativeErrorCode`）
- 存储：`StorageReadResult<T>` / `StorageWriteResult` / 状态枚举
- 配置：`ConfigurationLoadResult` / `ConfigurationSaveResult` / 状态枚举
- 排程：`NextExecutionResult` / `NextExecutionStatus`

### 4.3 与冻结架构 S13 的契约对照

| 架构契约 | 现状 | 结论 |
|----------|------|------|
| 8 态（waiting/running/confirming/executing/executed/cancelled/faulted/interrupted） | 现枚举为 9 值 V1 命名 | 需重写（计划内差距 D1） |
| 16 条合法转换白名单 | 现白名单为 V1 语义（含 Idle、自循环 Reschedule、终态→Idle） | 需重写（D1） |
| `TaskCollection` / `TaskArbitrator` / `CrashRecoveryManager` | 不存在 | 需新建（D2） |
| `tasks.json` 任务清单 | 不存在 | 需新建 schema（D3） |
| 多任务 `runtime.json` | 单实例 | 需重写（D2） |
| I1 默认不关机 / I2 唯一电源出口 | 满足（ShutdownWorkflow 唯一调 IPowerService） | 保持 |
| I3 UI 不拥有引擎 / I4 StageToken / I5 HasExecuted | 单实例满足 | 扩展为多实例 |
| I6 崩溃恢复 Interrupted | 内联于 SchedulerEngine | 迁出为 CrashRecoveryManager；注意 004 修正 2（waiting 不标记） |
| I7 配置损坏 Faulted / I8 双闸门 / I9 Mutex+Pipe | 满足 | 保持 |

> 注意：现代码枚举名与架构 4.4 的 V1↔V2 映射表不完全一致——代码用 `Warning`（≈V2 `confirming`）、`Completed`（≈V2 `executed`）、`Failed`（≈V2 `faulted`），且含映射表未出现的 `Idle/Scheduled`。实施时需逐态映射（属 D1）。

---

## 5. 当前工作包允许读取范围

**本轮已读取**（只读）：
- 工作区全部：`*.sln`、`*.props`、`NuGet.Config`、`README.md`
- `src/AutoShutdown.Core/**`、`src/AutoShutdown.App/**`、`tests/AutoShutdown.Tests/**` 全部源码
- `tools/`、`docs/`、`assets/`、`work/`、`.workbuddy/`、`artifacts/release/v1.0.0/`
- 上传规范文档（AI 执行规范、架构书 V2-DRAFT-003+004、S13 工作包卡）

**实施阶段建议可读**：以上全部，另加运行期 `%LocalAppData%\AutoShutdown\`（用于迁移/恢复验证，仅当实施需要）。

---

## 6. 建议允许修改范围（实施阶段）

| 文件 | 改动性质 |
|------|----------|
| `Core/State/Enums.cs` | 重写 `TaskState` 为 V2 8 态（或新增独立枚举）；保留 `PowerAction` |
| `Core/State/TaskStateMachine.cs` | 重写 V2 16 条转换白名单 |
| `Core/State/TaskTransitionCause.cs` / `TaskTransitionResult.cs` | 增补 V2 cause/决策码 |
| `Core/State/RuntimeState.cs` | 多实例集合；每实例独立 StageToken/HasExecuted/RealPowerConfirmed |
| `Core/Tasks/TaskDefinition.cs` / `TaskService.cs` / `ITaskService.cs` | 增优先级/启停字段；集合化 |
| `Core/Scheduling/SchedulerEngine.cs` / `SchedulerCommands.cs` / `SchedulerResults.cs` / `ISchedulerEngine.cs` | 多任务调度循环；命令定位到实例；Snapshot 多任务 |
| `Core/Workflow/ShutdownWorkflow.cs` / `IShutdownWorkflow.cs` | 仅状态枚举适配；不新增电源调用路径 |
| `Core/Configuration/AppConfig.cs` / `ConfigurationService.cs` / `IConfigurationService.cs` | 增 `tasks.json` 接入 |
| `Core/Storage/` | 新增 tasks.json 文档类型封装（可选） |
| `App/Presentation/MainWindowViewModel.cs` / `UiTextMapper.cs` / `MainWindow.xaml` / `DashboardRefreshService.cs` | 多任务列表 + 分别启停 + 状态指示切片 |
| `tests/AutoShutdown.Tests/**` | L0–L4 新增/改写 |

**建议新增文件**：
- `Core/Tasks/TaskCollection.cs`
- `Core/Recovery/CrashRecoveryManager.cs`
- `Core/Scheduling/TaskArbitrator.cs`
- `Core/State/TaskInstanceState.cs`（若采用独立枚举）
- UI 任务列表视图/视图模型

**共享边界提示**：`SchedulerEngine`（S14/S21/S22 共享）、`ShutdownWorkflow`（S15/S16/S20/S23 共享）、`TaskDefinition/持久化/schema`（S14/S15/S21/S22/S-PKG 共享）、UI 任务列表（S14/S15/S20 共享）。

---

## 7. 禁止边界

- 任何新模块不得调用 `IPowerService`（I2 唯一电源出口）
- 不得改变 `ShutdownWorkflow` 电源执行语义；`FakePowerService/Win32PowerService/GuardedPowerService/PowerRequest/PowerResult` 冻结
- `SingleInstanceCoordinator` / `ActivationPipeServer` 冻结（I9）
- 不得引入 S14 日历规则、S15 空闲规则、S16 Pre-Pipeline、S20 无人值守、S23 远程
- 不含任何 V3 功能（模板/导入导出/分布式/多语言/插件）
- 不修改冻结架构 I1–I9 定义；**仅当冻结架构无法实现或实施会破坏 I1~I9 时**，才提交架构变更单

---

## 8. 构建 / 测试 / 打包命令

> 环境能力不满足：本沙箱无 `dotnet`，无法执行。命令来源为 README 与 `tools/Publish-SafeRelease.ps1` 文本，**均未验证**。

```powershell
# 还原
dotnet restore .\AutoShutdown.sln

# 构建（Release，0 错误 0 警告门）
dotnet build .\AutoShutdown.sln -c Release --no-restore
# 或发布脚本使用：dotnet build .\AutoShutdown.sln -c Release --nologo -m:1 -p:UseSharedCompilation=false

# 测试（全量，solution 级）
dotnet test .\AutoShutdown.sln -c Release --no-build

# 单项目测试（注意真实路径为 .\tests\AutoShutdown.Tests\...）
dotnet test .\tests\AutoShutdown.Tests\AutoShutdown.Tests.csproj -c Release

# L0 状态机
dotnet test .\tests\AutoShutdown.Tests\AutoShutdown.Tests.csproj --filter "FullyQualifiedName~TaskStateMachineTests"
# L1 恢复/持久化
dotnet test .\tests\AutoShutdown.Tests\AutoShutdown.Tests.csproj --filter "FullyQualifiedName~SchedulerEngineTests"
dotnet test .\tests\AutoShutdown.Tests\AutoShutdown.Tests.csproj --filter "FullyQualifiedName~FileStorageTests"
# L2 集成/回归
dotnet test .\tests\AutoShutdown.Tests\AutoShutdown.Tests.csproj --filter "FullyQualifiedName~CorePipelineIntegrationTests"
dotnet test .\tests\AutoShutdown.Tests\AutoShutdown.Tests.csproj --filter "FullyQualifiedName~TaskServiceTests"

# 打包（Windows 主机）
.\tools\Publish-SafeRelease.ps1
# 产出 artifacts\release\v1.0.0\packages\AutoShutdown-v1.0.0-win-x64-{framework-dependent,self-contained}.zip
# 脚本内硬编码 SDK 路径：C:\Users\李佳茂\Documents\Codex\2026-08-10\new-chat-5\work\.dotnet-sdk\dotnet.exe
```

**候选 EXE**：命名已冻结为 `AutoShutdown-v2.0.0-S13.{BUILD}.exe`（架构 S-PKG §4）；当前脚本目标版本为 v1.0.0 且不重命名，构建号生成与保存目录待确认（见第 10.3 节 Q1/Q2）。

---

## 9. 当前 Gate 状态

| 闸门 | 状态 | 说明 |
|------|------|------|
| G0-S13 状态机设计评审（含崩溃恢复、仲裁） | 推定已完成，正式批准记录待绑定 | 依据冻结架构 V2-DRAFT-004；`.workbuddy/memory` 仅用于定位，不作为正式批准证据 |
| G0-S13 不变量 I1~I9 未破坏 | 推定已完成，正式批准记录待绑定 | 同上 |
| G0-S13《V2 UI 实施基线》 | **阻断**：真实文件未找到 | 工作区 `docs/` 仅见 `S11-main-visual.jpg`，未找到 UI 基线文档；**UI 任务开始前必须补齐** |
| 非 UI 代码扫描与 S13 细拆 | 可继续 | 不依赖 UI 基线 |
| G1-S13 多任务端到端验收 | 未开始 | 依赖实施 |
| G1-PKG-S13 首个候选 EXE + 冒烟 | 未开始 | 依赖实施 + Q1/Q2 决策 |

---

## 10. 未决事项与执行条件

### 10.1 计划内实施差距（D1~D4）——S13 既定工作量，非架构冲突

| # | 差距 | V1 现状 | V2 目标 |
|---|------|---------|---------|
| D1 | 状态枚举与转换表 | 9 值枚举、V1 转换表（含 Idle、自循环 Reschedule、终态→Idle） | 8 态 `waiting/running/confirming/executing/executed/cancelled/faulted/interrupted` + 16 条转换白名单 |
| D2 | 任务模型与调度 | 单实例 `CurrentInstance`、单任务调度循环、恢复逻辑内联 | `TaskCollection` + 多实例 `runtime.json` + `TaskArbitrator` + `CrashRecoveryManager` |
| D3 | 任务持久化 | 无 `tasks.json` | `tasks.json` 任务清单 schema |
| D4 | 命令/快照/UI | 命令无任务维度、Snapshot 单任务、UI 单任务卡片 | 多任务定位命令、多任务 Snapshot、任务列表与分别启停切片 |

这些差距正是 S13 工作包"范围"要求建立的内容；若现有代码已全部满足 V2，则反而不需要 S13。**仅在发现冻结架构无法实现、或实施会破坏 I1~I9 时**，才需要提交架构变更单。当前未发现此类情形。

### 10.2 从冻结架构可提取的已确定结论（无需人工裁决）

| 结论 | 依据 |
|------|------|
| 候选 EXE 命名冻结为 `AutoShutdown-v2.0.0-S13.{BUILD}.exe` | 架构 S-PKG §4 |
| Pre-Pipeline 属于 S16，S13 明确不含 | 架构第 5 章总览表 + S13 工作包"明确不含" |
| 崩溃恢复仅标记 `running/confirming/executing`，`waiting` 保留并重算排程 | V2-DRAFT-004 修正 2 |
| `waiting → cancelled` 含"系统判定一次性任务过期"触发条件 | V2-DRAFT-004 修正 6d |
| ARCH-CR-001 / CR-002 已批准（DEC-001/DEC-002） | 审查确认；与 S13 无直接关系，S20/S23 开始前重新核对批准条件 |

### 10.3 真正待人工确认的（仅剩余项）

| # | 待确认 | 影响 |
|---|--------|------|
| Q1 | 构建号 `{BUILD}` 的生成规则（递增方式/来源） | 候选 EXE 命名与可追溯性 |
| Q2 | 候选 EXE 保存目录（沿用 `artifacts/release/v1.0.0/` 还是新布局） | S-PKG 衔接 |
| Q3 | 旧 `runtime.json` 的处理策略（迁移 / 备份后重建 / 其他冻结策略） | 升级与恢复路径 |
| Q4 | `TaskDefinition.Priority` 字段形式是否已在契约中固定（类型 / 默认 / 范围） | `TaskArbitrator` 实现 |

### 10.4 环境阻断（E1）

| # | 阻断 | 处理 |
|---|------|------|
| E1 | 当前沙箱无 .NET SDK，无法执行 `dotnet restore/build/test/publish` | 换到具备 **Windows + .NET 8 SDK** 的环境执行验证 |

属执行环境条件，不是产品或架构裁决项，无需人工做方案决定。

### 10.5 执行限制声明

- 阶段 H 的编码与验证**必须**换到能够读取、修改真实 Windows 项目，并实际运行 `dotnet build/test/publish` 的环境。
- 本文档再详细也不能替代真实编译与测试；上述命令在当前环境全部为"未验证"。

---

## 附：事实分层

**可自动发现的技术事实（已全部识别）**：源码/解决方案标识、技术栈与目标平台、启动链、全部相关符号与接口签名、状态枚举与转换表、持久化 schema、22 个测试文件的覆盖、构建/测试/打包命令及来源、既有发布产物。

**从冻结架构可提取的确定结论（第 10.2 节）**：EXE 命名、Pre-Pipeline 归属、崩溃恢复范围、一次性过期触发、CR-001/002 已批准。

**真正待人工确认（第 10.3 节）**：Q1–Q4（构建号、EXE 保存目录、runtime.json 策略、Priority 契约形式）。

**环境能力不满足**：构建、测试、打包均无法在当前沙箱实际运行；版本控制不可用。
