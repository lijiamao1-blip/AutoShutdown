# S15 结果记录（空闲关机）

> 执行书：`S15-work包/AutoShutdown-V2-S15-阶段一键执行书.docx`
> 执行日期：2026-08-16
> 范围：S15 全阶段（检查点 1~3）。阶段完成后停止，未进入 S16。
> 前置：S14 已通过并冻结复杂排程 / V2 模型 / 8 态状态机 / 仲裁契约。

---

## 1. 阶段结论

**结论：通过。**

| 验收条件（执行书） | 状态 |
|------|------|
| 空闲检测：GetLastInputInfo + 32 位 tick 回绕 | ✅ `IdleTickMath.ComputeIdleMilliseconds`（unchecked uint 减法）+ `Win32IdleInputSource` |
| 阈值判定：任务阈值 >0，否则继承全局默认（30 分钟） | ✅ `IdleShutdownRule.ResolveThreshold` / `IsIdleDue` |
| 空闲触发 → 倒计时/确认窗口，同刻到期进入统一仲裁 | ✅ `ArmIdleAsync` + 现有 `TaskArbitrator`（两空闲任务同刻到期仲裁测试） |
| 输入恢复取消倒计时，不触发电源 | ✅ `CancelIdleTriggeredAsync`（Confirming→Cancelled）+ `IsIdleRecovered` 标记 |
| 检测失败 fail-closed：不触发、不取消，下次循环重试 | ✅ `IdleDuration=null` 分支（保持 Waiting） |
| 非空闲任务完全不受影响 | ✅ `IsIdleInstance` 门控 + `NonIdleTask_UnaffectedByIdleMonitor` |
| UI：阈值/继承、倒计时来源、恢复取消结果 | ✅ `TimeMode.Idle` + 阈值下拉 + `CountdownSourceText` + `已取消（输入恢复）` |
| 自动化不触发真实电源 | ✅ 全程替身（FakeHandler / NoOp 仲裁 / FakePower） |
| TreatWarningsAsErrors | ✅ Release build 0 警告 0 错误 |

---

## 2. 实施顺序（3 个检查点）

| 检查点 | 内容 | 提交 |
|------|------|------|
| C1 | 空闲检测抽象 + IdleShutdownRule + TaskKind.Idle + 阈值字段/校验 + 原生互操作收敛 | `b25273a` |
| C2 | 调度器空闲评估 + 输入恢复取消 + DI 接线 + 集成测试 | `5053965` |
| C3 | UI 切片（TimeMode.Idle / 阈值继承 / 倒计时来源 / 恢复取消结果）+ 两阈值测试 | `aa8bce9` |

---

## 3. 交付物

### 3.1 核心（AutoShutdown.Core）

| 文件 | 职责 |
|------|------|
| `Abstractions/IIdleInputSource.cs` | 空闲时长输入源（null=检测失败） |
| `Abstractions/IIdleMonitor.cs` | 可启停的空闲监视器封装 |
| `Idle/IdleMonitor.cs` | 包装 `IIdleInputSource`，非监控态返回 null |
| `Idle/IdleShutdownRule.cs` | `GlobalDefaultThreshold`（30 分钟）+ `ResolveThreshold` + `IsIdleDue` |
| `State/Enums.cs` | `TaskKind.Idle = 8` |
| `Tasks/TaskDefinition.cs` | 新增 `IdleThresholdSeconds`（可空，null=继承全局默认） |
| `Tasks/TaskDefinitionValidator.cs` | 空闲阈值 1..604800 校验 |
| `Scheduling/NextExecutionResult.cs` | `MissingIdleThreshold` 结果 |
| `Scheduling/NextExecutionCalculator.cs` | Idle 占位 fire time（now+阈值，仅供展示） |
| `State/RuntimeState.cs` | `TaskInstance.IsIdleTriggered` / `IsIdleRecovered` 标记 |
| `Scheduling/SchedulerEngine.cs` | 1s 轮询驱动空闲评估；`TryEvaluateIdleAsync` / `ArmIdleAsync` / `CancelIdleTriggeredAsync` / `IsIdleInstance`；Waiting 占位 fire time 不计墙钟 due/deadline |

### 3.2 应用层（AutoShutdown.App）

| 文件 | 职责 |
|------|------|
| `Infrastructure/Idle/IdleTickMath.cs` | `ComputeIdleMilliseconds`（unchecked uint 减法，正确处理 49.7 天回绕） |
| `Infrastructure/Idle/Win32IdleInputSource.cs` | 委托 `Win32IdleNativeApi`，无直接 DllImport |
| `Infrastructure/Power/Win32PowerNativeApi.cs` | 新增 `Win32IdleNativeApi.GetLastInputInfo`（保持唯一 P/Invoke 网关契约） |
| `AppHost/ServiceRegistration.cs` | `IIdleInputSource` / `IIdleMonitor` 注册 + SchedulerEngine 工厂接线 |
| `Presentation/UiTextMapper.cs` | `Map(TaskInstance)`：输入恢复取消单列「已取消（输入恢复）」 |
| `Presentation/MainWindowViewModel.cs` | `TimeMode.Idle` + 阈值下拉（继承/显式）+ `CountdownSourceText` + 列表 `TriggerText` |
| `MainWindow.xaml` | 空闲触发单选 + 阈值下拉 + 当前任务「触发方式」行 + 列表「触发」列 |

### 3.3 测试（AutoShutdown.Tests）

| 文件 | 职责 |
|------|------|
| `S15_IdleDetectionRuleTests.cs`（新增） | 23 项：阈值解析/继承/越界、Idle 判定、tick 回绕、检测失败 fail-closed |
| `S15_SchedulerIdleIntegrationTests.cs`（新增） | 10 项：触发、低于阈值、检测失败、继承全局默认、无/有告警执行、输入恢复取消、非空闲不受影响、两空闲同刻仲裁、两阈值任务 |
| `S15_Checkpoint3_UiSliceTests.cs`（新增） | 7 项：Idle 字段映射（继承/显式）、TryBuildTimeInput 无时长目标、可见性标志、恢复取消结果文本、倒计时来源/列表触发列 |

---

## 4. 测试与构建结果

```
$ dotnet test -c Release --filter "FullyQualifiedName~S15"
  已通过! - 失败: 0，通过: 40，已跳过: 0，总计: 40
```

```
$ dotnet test -c Release
  已通过! - 失败: 0，通过: 658，已跳过: 0，总计: 658
```

```
$ dotnet build -c Release
  已成功生成。0 个警告，0 个错误
```

（S15 全阶段累计 658 = S14 基线 618 + 新增 40，全部通过。）
（注：全量测试首次运行出现 1 个既有时序敏感测试 `CorePipelineIntegrationTests.G_CommandAndDeadlineTogether_CommandWins_NoExecution` 超时；隔离重跑与二次全量均通过，非 S15 引入。）

---

## 5. 提交清单

| 提交 | 说明 |
|------|------|
| `c2440a1` | S15 起点（S14 结果记录，已冻结） |
| `b25273a` | S15-C1: idle detection + IdleShutdownRule + TaskKind.Idle model |
| `5053965` | S15-C2: scheduler idle evaluation + input-recovery cancel + DI wiring |
| `aa8bce9` | S15-C3: idle UI slice + recovery-cancel marker + two-threshold tests |

工作树：干净（仅余参考文档 `S13-work包/…docx`、`S14-work包/…docx`、`S14-work包/S13-T08T09-结果记录.md`、`S15-work包/执行书与 S14 结果记录参考`，均非交付物，不纳入提交）。

---

## 6. 端到端验收证据

| 环节 | 证据 |
|------|------|
| 空闲检测 | `S15_IdleDetectionRuleTests`（tick 回绕 23 项）+ `Win32IdleInputSource` 委托 `Win32IdleNativeApi` |
| 阈值继承 | `IdleTask_WithoutOwnThreshold_InheritsGlobalDefault`（无任务阈值→继承全局默认 10 分钟触发） |
| 触发→倒计时 | `IdleTask_WhenIdleReachesThreshold_ArmsToConfirming`（ScheduledFireTime=now+告警，IsIdleTriggered=true） |
| 到期仲裁 | `TwoIdleTasks_DueSimultaneously_EnterUnifiedArbitration`（2 候选进入统一仲裁，不并发直发电源） |
| 输入恢复取消 | `IdleTask_InputRecoveryDuringCountdown_Cancels`（空闲回落→Cancelled，0 次电源）+ `IsIdleRecovered` 标记 |
| 恢复取消结果呈现 | `S15_Checkpoint3_UiSliceTests.Refresh_RecoveryCancelled_SetsRecoveryResultStateText`（「已取消（输入恢复）」） |
| 两阈值任务 | `TwoIdleTasks_DifferentThresholds_OnlyReachedArms`（仅达到阈值任务触发，另一保持 Waiting） |
| 非空闲不受影响 | `NonIdleTask_UnaffectedByIdleMonitor`（倒计时任务照常执行） |

---

## 7. 遗留 / 开放项（≤3，不阻断）

1. **真机电源未执行**：全阶段自动化测试均使用替身（FakeHandler / NoOp 仲裁 / FakePower）；真实关机/重启/休眠未在真机执行，需人工按隔离步骤验证（见 §9）。
2. **输入恢复取消为终态 Cancelled，不自动重新武装**：冻结状态机无 Confirming→Waiting 白名单边，故恢复后实例终结、任务定义保留（不删除），需用户重新创建；若后续要求「恢复后自动重新武装」，需单独裁决（不属本阶段范围）。
3. **UI 仅 VM + XAML 切片**：`MainWindow.xaml` 绑定依赖 WPF 运行时解析，未做真机 UI 冒烟/截图。

---

## 8. 禁止边界执行声明

- 未修改 `PowerRequest` / `PowerResult` / `GuardedPowerService` / `Win32PowerService` / `FakePowerService` / `ShutdownWorkflow` / 唯一电源出口；`ShutdownWorkflow` 仍是唯一调用 `IPowerService` 的模块（结构性测试锁定）。
- 未修改冻结的 8 态状态机白名单；输入恢复取消复用 `Confirming→Cancelled`（`CancelledDuringConfirmation`），仅字段级新增 `IsIdleTriggered` / `IsIdleRecovered`。
- DllImport 仍收敛于 `Win32PowerNativeApi.cs` 单一网关（源码结构契约测试锁定）。
- 自动化测试全部使用替身电源，未触发真实关机/重启/睡眠/休眠。
- 未修改 S13/S14 冻结契约、仲裁或调度循环所有权。
- 未触碰 S16 范围。

---

## 9. 未执行 / 人工待验（真机项）

以下项目未执行（环境无安全授权/隔离真机），**明确标注为未执行**，不写为通过：

1. 真实电源操作冒烟（关机/重启/睡眠/休眠）。
2. 真机 WPF UI 手动冒烟与截图（空闲触发单选、阈值下拉、倒计时来源、恢复取消结果展示）。
3. 真机空闲输入源（GetLastInputInfo）实测：真实移动鼠标/键盘后倒计时取消。

---

## 10. 是否可进入 S16

**是。** S15 三检查点全部通过、Release 全量测试（658）与构建（0 警告 0 错误）干净、工作树干净（仅余参考/执行文档）、遗留项均不阻断。可按执行书将本回报包交独立验收后新开对话进入 S16。
