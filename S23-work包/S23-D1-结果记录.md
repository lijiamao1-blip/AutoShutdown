# AUTOSHUTDOWN V2
# S23-D1 结果记录（独立返修记录）
## 局域网远程控制 —— 远程拒绝服务边界与 HMAC 格式注释

> 本文件是 S23-D1 最小返修的**独立结果记录**。原 `S23-结果记录.md` 为历史证据，未被改写、覆盖或提交替换（见「原 S23 结果记录核验」）。

## 1. 返修结论

**通过。**

两处安全/文档缺口（远程拒绝服务边界、HMAC 格式注释矛盾）均已修复并有真实测试覆盖；原 S23 相关测试无回归；冻结边界（唯一电源出口、双闸门、白名单只读、TLS+无人值守强制）保持不变。

## 2. 提交与工作树

| 项 | 值 |
|---|---|
| 起始提交（返修基线） | `master / 8be14e6`（S23 result record: LAN remote control complete） |
| D1 实现提交 | `5418e25`（S23-D1: remote DoS boundaries — nonce atomic reserve + connection deadlines/cap + HMAC comment fix） |
| D1 结果记录提交 | 本记录单独提交（见末节提交清单） |
| 结束提交 | 见「提交清单」 |
| 最终工作树状态 | 仅剩余预先存在的未跟踪工作包与 LibreOffice MSI（S13–S23-work包、LibreOffice_26.2.5_Win_x86-64.msi 等）；D1 全部改动已提交；无冲突、无重置、无删除、无覆盖。**未 git push。** |

**返修内提交清单**

1. `5418e25` — S23-D1 实现 + 边界测试（5 个文件：4 个源码 + 1 个新测试）。
2. （独立）— S23-D1 结果记录（本文件，单独提交）。

## 3. 实际修改文件及职责

| 文件 | 职责 |
|---|---|
| `src/AutoShutdown.Core/Remote/RemoteAuthenticator.cs` | nonce 仅在「设备存在 + HMAC 验证通过」后原子保留；普通字典 + `lock(_nonceGate)` 保证「清理过期 → 容量检查 → 添加」并发原子；未过期条目达到 10,000 拒绝新 nonce（绝不越限）；合法重放仍 fail-closed。 |
| `src/AutoShutdown.App/Infrastructure/Remote/RemoteServer.cs` | 对首字节读取、TLS 握手、整行读取设置固定、受测的连接期限（`.WaitAsync`）；在途连接上限（`SemaphoreSlim`，超限立即关闭新连接）；期限到期 / 不完整帧 / 并发超限一律关闭连接，不调用 `RemoteRequestHandler`、不提交 `SchedulerEngine`、不执行任何电源动作。 |
| `src/AutoShutdown.Core/Remote/RemoteMessages.cs` | 仅修正 HMAC 编码注释：`base64(HMAC-SHA256(...))` → `hex 小写(HMAC-SHA256(...))`；实现与 RC 协议零改动。 |
| `src/AutoShutdown.Core/Remote/RemoteProtocol.cs` | 新增连接资源常量：`ConnectionFirstByteTimeout`/`ConnectionTlsHandshakeTimeout`/`ConnectionLineReadTimeout`（各 10s）、`MaxConcurrentConnections`（64）；`NonceCacheMaxEntries` 注释补充「绝不越限」。 |
| `tests/AutoShutdown.Tests/S23_D1_RemoteBoundaryTests.cs` | 新增 9 项边界测试（见 §4），覆盖执行书检查点 5 全部 7 项要求。 |

原 S23 执行书、原 `S23-结果记录.md` 未改动。

## 4. 测试与构建原始数量 / 退出码

| 步骤 | 结果 | 退出码 |
|---|---|---|
| Debug 构建（tests 项目） | Build succeeded, 0 Warning, 0 Error | 0 |
| S23-D1 边界测试（Debug，`--filter FullyQualifiedName~S23_D1_RemoteBoundaryTests`） | Passed: 9 / 0 Failed / 0 Skipped | 0 |
| S23 全部相关测试（Debug，`--filter FullyQualifiedName~S23_`） | Passed: 109 / 0 Failed / 0 Skipped | 0 |
| Release restore + build | Build succeeded, 0 Warning, 0 Error | 0 |
| Release 全量测试 | Passed: 1442 / 0 Failed / 0 Skipped | 0 |
| DPAPI 测试（Release，`--filter FullyQualifiedName~DpapiProtector`） | Passed: 2 / 0 Failed / 0 Skipped | 0 |
| `git diff --check` | 无空白错误（仅 Windows LF→CRLF 提示） | 0 |

**DPAPI 测试身份说明**：`DpapiProtector_RoundTrip_RestoresPlaintext` / `DpapiProtector_Garbage_Throws_FailClosed` 在**已加载 Windows 用户配置的真实桌面身份**（登录用户）下复跑，`ProtectedData` CurrentUser 作用域调用成功，证明并非在不可靠身份下跳过或假通过。

**新增 D1 边界测试清单（9 项，全部通过）**

1. `Auth_BadHmacAndUnknownDevice_DoNotConsumeNonce` — 非法/未知设备/HMAC 失败请求不占用 nonce（容量 2 下两次失败后两个合法请求仍成功）。
2. `Auth_Capacity_RejectsNewNonceThenFreesAfterEviction` — 容量边界：满后拒绝新 nonce；时钟推进 TTL 后过期项被清理、空间释放。
3. `Auth_Concurrent_DoesNotExceedCapacity` — 容量 50、300 并发合法请求：恰好 50 成功、250 拒绝，缓存绝不越界。
4. `Auth_ValidReplay_StillRejected` — 合法重放仍被拒绝（fail-closed）。
5. `Auth_RealCapacity_10000_RejectsAtBoundary` — 真实 10,000 容量：前 10,000 全成功、第 10,001 个在容量边界被拒。
6. `Server_SlowFirstByte_ClosesWithinDeadline_NoHandlerNoEngine` — 慢首字节期限内关闭，无 handler/引擎/电源副作用。
7. `Server_SlowLineAndIncompleteFrame_ClosesWithinDeadline_NoHandlerNoEngine` — 慢行帧/不完整帧期限内关闭，无副作用。
8. `Server_TlsHandshakeStall_ClosesWithinDeadline_NoHandlerNoEngine` — TLS 握手停滞期限内关闭，无副作用。
9. `Server_ConcurrentConnections_AtLimit_NewConnectionClosed_NoHandlerNoEngine` — 并发上限超限连接立即关闭，无副作用。

## 5. 关键安全不变量核对

| 不变量 | 核对结果 |
|---|---|
| nonce 只在「设备存在 + HMAC 通过」后原子保留；非法/未知设备/HMAC 失败不占 nonce | ✅ 通过（实现先验证明 + 测试 1） |
| 清理过期项后未过期条目达到 10,000 → 拒绝新 nonce，绝不越限；并发不越界 | ✅ 通过（测试 2/3/5） |
| 合法重放仍被拒绝（fail-closed） | ✅ 通过（测试 4） |
| 慢首字节 / 慢行帧 / TLS 握手停滞 / 并发上限 → 均在期限内关闭连接，无 handler 调用、无引擎提交、无电源副作用 | ✅ 通过（测试 6–9；断言 `Engine.Submitted` 为空且无 handler 审计条目） |
| `ShutdownWorkflow` 唯一 `IPowerService` 出口 | ✅ 未改动；相关模块零变更 |
| 双闸门、白名单只读、TLS+无人值守强制要求 | ✅ 未改动；`RemoteRequestHandler`、白名单、无人值守策略代码零变更 |
| 原 S23 相关测试无回归 | ✅ 109 项 S23 测试全部通过 |
| 原 `S23-结果记录.md` / 原 S23 执行书原样保留 | ✅ `git diff HEAD -- S23-work包/` 为空；结果记录仍为 `8be14e6` 提交内容 |

## 6. 未执行 / 人工待验项目

- **真机局域网多客户端验证**（真实局域网客户端连接期限 / 并发上限 / 拒绝服务防护）：本环境仅能本机回环自测，无法代表真实局域网多端并发。**未执行 / 人工待验**。
  - 人工步骤：两台真实机器，服务端开启远程控制（RequireTls=true）；① 用脚本同时发起 65+ 个连接，观察第 65 个之后被立即关闭且任务管理器 / 日志无引擎提交；② 用一个只发首字节不完成握手的客户端，确认约 10s 后连接被关闭；③ 用一个发一半请求行后停滞的客户端，确认约 10s 后连接被关闭；④ 确认审计日志记录来源 IP 且无 PIN/secret/HMAC。

## 7. 遗留问题（最多 3 项）

1. 真机局域网多客户端并发 / 慢连接防护验证尚未执行（人工待验，见 §6）。
2. 无其它已确认问题。

## 8. 是否可进入下一阶段

**不进入 S24。** 本 D1 返修验收通过后即停止，等待独立复验。验收结论由外部复验方判定。

## 9. 原 S23 结果记录核验

`S23-work包/S23-结果记录.md` 提交于 `8be14e6`，本次返修**未改写、未覆盖、未提交替换**；`git diff HEAD -- "S23-work包/"` 为空，证明原文件零改动。
